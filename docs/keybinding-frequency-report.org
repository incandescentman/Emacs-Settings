:PROPERTIES:
:ID: keybinding-frequency-report
:END:
#+title: Keybinding Frequency Report
#+date: [2026-02-10 Tue]

Auto-generated by =M-x my/keybinding-frequency-report=.

* Most Used Commands

- =right-word= (11729 uses) :: (no custom binding) | no binding
- =smart-space= (7018 uses) :: (no custom binding) | no binding
- =left-word= (6353 uses) :: (no custom binding) | no binding
- =org-shiftdown= (4872 uses) :: (no custom binding) | no binding
- =org-shiftup= (4731 uses) :: (no custom binding) | no binding
- =my/delete-backward-and-capitalize= (2551 uses) :: ~<backspace>~ | single-key
- =jay/save-all-buffers= (2338 uses) :: ~s-s~ | single-key
- =smart-return= (2094 uses) :: (no custom binding) | no binding
- =jay/right-char= (1636 uses) :: (no custom binding) | no binding
- =jay/left-char= (1454 uses) :: (no custom binding) | no binding
- =pasteboard-paste-adaptive= (851 uses) :: ~s-v~ | single-key
- =isearch-printing-char= (683 uses) :: (no custom binding) | no binding
- =mac-mwheel-scroll= (591 uses) :: (no custom binding) | no binding
- =kill-word-correctly-and-capitalize= (559 uses) :: ~C-d~ | single-key
- =pasteboard-copy-adaptive= (517 uses) :: ~s-c~ | single-key
- =comma-or-smart-comma= (515 uses) :: (no custom binding) | no binding
- =smart-period= (453 uses) :: (no custom binding) | no binding
- =undo-fu-only-undo= (437 uses) :: ~s-z~ | single-key
- =beginning-of-buffer= (406 uses) :: ~<s-up>~ | single-key
- =backward-kill-word-correctly-and-capitalize= (314 uses) :: ~M-<backspace>~ | single-key
- =pasteboard-cut-adaptive= (290 uses) :: ~s-x~ | single-key
- =other-window-or-frame= (272 uses) :: ~s-`~ | single-key
- =end-of-buffer= (261 uses) :: ~<s-down>~ | single-key
- =mwim-end= (249 uses) :: ~C-e~ | single-key
- =narrow-or-widen-dwim= (243 uses) :: ~s-b~ | single-key
- =my/kill-line-dwim= (239 uses) :: ~C-k~ | single-key
- =ivy-next-line= (236 uses) :: (no custom binding) | no binding
- =mark-whole-buffer= (231 uses) :: ~s-a~ | single-key
- =kill-current-buffer= (220 uses) :: ~s-k s-k~ / ~s-k RET~ | double-key
- =isearch-repeat-forward= (213 uses) :: ~s-g~ | single-key
- =org-shiftleft= (210 uses) :: (no custom binding) | no binding
- =org-cycle= (203 uses) :: (no custom binding) | no binding
- =vertico-next= (159 uses) :: (no custom binding) | no binding
- =org-shiftright= (149 uses) :: (no custom binding) | no binding
- =mwim-beginning= (135 uses) :: ~C-a~ | single-key
- =buffer-stack-down= (132 uses) :: ~<s-right>~ | single-key
- =isearch-forward-ignore-case= (127 uses) :: ~s-f~ | single-key
- =vertico-exit= (127 uses) :: (no custom binding) | no binding
- =new-buffer= (124 uses) :: ~s-t~ | single-key
- =down-by-degrees= (117 uses) :: (no custom binding) | no binding
- =capitalize-or-endless/capitalize= (114 uses) :: ~M-c~ | single-key
- =previous-buffer= (91 uses) :: ~M-s-<left>~ | single-key
- =up-by-degrees= (86 uses) :: (no custom binding) | no binding
- =colon-or-smart-colon= (86 uses) :: (no custom binding) | no binding
- =downcase-or-endless-downcase= (85 uses) :: ~M-l~ | single-key
- =smart-exclamation-point= (83 uses) :: (no custom binding) | no binding
- =org-export-dispatch= (69 uses) :: ~s-p~ | single-key
- =keyboard-quit= (68 uses) :: (no custom binding) | no binding
- =path-copy-path-to-clipboard= (66 uses) :: ~s-|~ | single-key
- =endless/upcase= (65 uses) :: ~M-u~ | single-key

* Ergonomic Mismatches (high frequency, awkward binding)


* Underused Prime Keys (single s-KEY, low frequency)

- ~s-R~ :: =fasd-find-file= (5 uses) -- prime real estate, rarely used
- ~s-r~ :: =counsel-recentf= (0 uses) -- prime real estate, rarely used
- ~s-P~ :: =projectile-find-file= (0 uses) -- prime real estate, rarely used
- ~s-F~ :: =pasteboard-search-for-clipboard-contents= (0 uses) -- prime real estate, rarely used
- ~s-h~ :: =replace-string= (6 uses) -- prime real estate, rarely used
- ~s-n~ :: =make-frame= (0 uses) -- prime real estate, rarely used
- ~s-O~ :: =reveal-in-finder= (8 uses) -- prime real estate, rarely used
- ~s-d~ :: =org-todo= (3 uses) -- prime real estate, rarely used
- ~s-j~ :: =org-todo= (3 uses) -- prime real estate, rarely used
- ~s-S~ :: =org-mac-link-skim-insert= (1 uses) -- prime real estate, rarely used
- ~s-B~ :: =consult-buffer= (3 uses) -- prime real estate, rarely used
- ~s-I~ :: =clone-indirect-buffer-new-window-and-focus= (1 uses) -- prime real estate, rarely used
- ~s-D~ :: =define-word-at-point= (0 uses) -- prime real estate, rarely used
- ~s-e~ :: =embark-act= (0 uses) -- prime real estate, rarely used
- ~s-i~ :: =imenu= (1 uses) -- prime real estate, rarely used
- ~s-E~ :: =new-email-from-subtree-no-signature= (0 uses) -- prime real estate, rarely used
- ~s-M~ :: =tr-toggle-transclusion= (1 uses) -- prime real estate, rarely used
- ~s-W~ :: =open-weeklies= (0 uses) -- prime real estate, rarely used
