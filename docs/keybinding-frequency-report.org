:PROPERTIES:
:ID: keybinding-frequency-report
:END:
#+title: Keybinding Frequency Report
#+date: [2026-02-10 Tue]

Auto-generated by =M-x my/keybinding-frequency-report=.
Bindings are resolved from active keymaps for each recorded major mode (global + local + minor).

* Most Used Commands

- =right-word= (23463 uses) :: ~M-<right>~ / ~C-<right>~ / ~C - < r i g h t >~ / ~M - < r i g h t >~ | single-key
- =smart-space= (14049 uses) :: ~SPC~ | single-key
- =left-word= (12706 uses) :: ~M-<left>~ / ~C-<left>~ / ~C - < l e f t >~ / ~M - < l e f t >~ | single-key
- =org-shiftdown= (9744 uses) :: ~S-<down>~ / ~M-S-<down>~ / ~C-c <down>~ | single-key
- =org-shiftup= (9468 uses) :: ~S-<up>~ / ~M-S-<up>~ / ~C-c <up>~ | single-key
- =my/delete-backward-and-capitalize= (5106 uses) :: ~<backspace>~ | single-key
- =jay/save-all-buffers= (4681 uses) :: ~s-s~ | single-key
- =smart-return= (4191 uses) :: ~<return>~ | single-key
- =jay/right-char= (3292 uses) :: ~<right>~ | single-key
- =jay/left-char= (2911 uses) :: ~<left>~ | single-key
- =pasteboard-paste-adaptive= (1701 uses) :: ~s-v~ | single-key
- =isearch-printing-char= (1366 uses) :: (no binding found) | no binding
- =mac-mwheel-scroll= (1182 uses) :: (no binding found) | no binding
- =kill-word-correctly-and-capitalize= (1118 uses) :: ~C-d~ | single-key
- =comma-or-smart-comma= (1030 uses) :: ~,~ | single-key
- =pasteboard-copy-adaptive= (1030 uses) :: ~s-c~ | single-key
- =smart-period= (907 uses) :: ~.~ | single-key
- =undo-fu-only-undo= (877 uses) :: ~s-z~ | single-key
- =beginning-of-buffer= (812 uses) :: ~M-<~ / ~s-<up>~ / ~<begin>~ / ~C-<home>~ | single-key
- =backward-kill-word-correctly-and-capitalize= (628 uses) :: ~M-<backspace>~ | single-key
- =pasteboard-cut-adaptive= (580 uses) :: ~s-x~ | single-key
- =other-window-or-frame= (543 uses) :: ~s-`~ | single-key
- =end-of-buffer= (522 uses) :: ~M->~ / ~C-<end>~ / ~s-<down>~ | single-key
- =mwim-end= (500 uses) :: ~C-e~ / ~C - e~ | single-key
- =narrow-or-widen-dwim= (486 uses) :: ~s-b~ | single-key
- =my/kill-line-dwim= (485 uses) :: ~C-k~ | single-key
- =ivy-next-line= (472 uses) :: (no binding found) | no binding
- =mark-whole-buffer= (455 uses) :: ~s-a~ / ~C-x h~ | single-key
- =org-shiftleft= (442 uses) :: ~S-<left>~ / ~C-c <left>~ | single-key
- =kill-current-buffer= (440 uses) :: ~s-k RET~ / ~s-k s-k~ | double-key
- =isearch-repeat-forward= (426 uses) :: ~s-g~ | single-key
- =org-cycle= (406 uses) :: ~TAB~ | single-key
- =vertico-next= (314 uses) :: (no binding found) | no binding
- =org-shiftright= (299 uses) :: ~S-<right>~ / ~C-c <right>~ | single-key
- =mwim-beginning= (273 uses) :: ~C-a~ / ~C - a~ | single-key
- =buffer-stack-down= (264 uses) :: ~s-<right>~ | single-key
- =isearch-forward-ignore-case= (254 uses) :: ~s-f~ | single-key
- =vertico-exit= (253 uses) :: (no binding found) | no binding
- =new-buffer= (246 uses) :: ~s-t~ | single-key
- =down-by-degrees= (234 uses) :: ~M-<down>~ | single-key
- =capitalize-or-endless/capitalize= (228 uses) :: ~M-c~ | single-key
- =previous-buffer= (181 uses) :: ~<XF86Back>~ / ~M-s-<left>~ / ~C-x <left>~ / ~C-x C-<left>~ | single-key
- =colon-or-smart-colon= (172 uses) :: ~:~ | single-key
- =up-by-degrees= (172 uses) :: ~M-<up>~ | single-key
- =downcase-or-endless-downcase= (170 uses) :: ~M-l~ | single-key
- =smart-exclamation-point= (166 uses) :: ~!~ | single-key
- =org-export-dispatch= (138 uses) :: ~s-p~ / ~C-c C-e~ | single-key
- =keyboard-quit= (135 uses) :: ~C-g~ | single-key
- =path-copy-path-to-clipboard= (132 uses) :: ~s-|~ | single-key
- =endless/upcase= (130 uses) :: ~M-u~ | single-key

* Ergonomic Mismatches (high frequency, awkward binding)

- =delete-backward-char= (106 uses) :: ~D E L~ -- triple-key
- =exit-minibuffer= (56 uses) :: ~T A B~ / ~S P C~ / ~R E T~ / ~C - j~ -- triple-key
- =backward-kill-word= (32 uses) :: ~M - D E L~ / ~C - < b a c k s p a c e >~ -- quad-key
- =counsel-projectile-ag= (32 uses) :: ~s - / SPC p SPC a~ -- quad-key
- =org-mode= (24 uses) :: ~s-k o m~ -- triple-key

* Underused Prime Keys (single s-KEY, low frequency)

- Reserved (excluded): ~s-d~ (Command-Tab (macOS app switcher))

- ~s-r~ :: =counsel-recentf= (2 uses) -- prime real estate, rarely used
- ~s-P~ :: =projectile-find-file= (0 uses) -- prime real estate, rarely used
- ~s-F~ :: =pasteboard-search-for-clipboard-contents= (0 uses) -- prime real estate, rarely used
- ~s-n~ :: =make-frame= (0 uses) -- prime real estate, rarely used
- ~s-j~ :: =org-todo= (6 uses) -- prime real estate, rarely used
- ~s-S~ :: =org-mac-link-skim-insert= (2 uses) -- prime real estate, rarely used
- ~s-B~ :: =consult-buffer= (6 uses) -- prime real estate, rarely used
- ~s-I~ :: =clone-indirect-buffer-new-window-and-focus= (2 uses) -- prime real estate, rarely used
- ~s-D~ :: =define-word-at-point= (0 uses) -- prime real estate, rarely used
- ~s-e~ :: =embark-act= (1 uses) -- prime real estate, rarely used
- ~s-i~ :: =imenu= (2 uses) -- prime real estate, rarely used
- ~s-E~ :: =new-email-from-subtree-no-signature= (0 uses) -- prime real estate, rarely used
- ~s-M~ :: =tr-toggle-transclusion= (1 uses) -- prime real estate, rarely used
- ~s-W~ :: =open-weeklies= (0 uses) -- prime real estate, rarely used
