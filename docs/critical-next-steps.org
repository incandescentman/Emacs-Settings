#+TITLE: Critical Next Steps & Simplification Roadmap
#+DATE: 2025-11-26
#+DESCRIPTION: Technical debt, improvements, simplification plan, and future roadmap

* Overview

This document tracks:
- Active technical debt that should be addressed
- Planned improvements and why they matter
- Simplification opportunities with implementation details
- Stretch goals for future consideration
- Things explicitly NOT being pursued (and why)

Status indicators:
- *[ACTIVE]* - Currently important, should address soon
- *[PLANNED]* - Scoped and ready to implement
- *[COMPLETED]* - Done
- *[OPTIONAL]* - Would be nice, but not critical
- *[TABLED]* - Decided not to pursue (for now)

*Progress:* See progress table at bottom. Modularization work is now a major ongoing track alongside the original simplification roadmap.

* Next Modularization Queue

** [PLANNED] Pause point after the Org-roam sidecar and docs normalization checkpoints
- Resume from the current modularization state rather than opening a new front.
- First priority if we continue modularizing: revisit the remaining agenda/workflow residue in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] only if a clear existing-module home emerges in [[file+emacs:/Users/jay/emacs/emacs-settings/goals-agenda.org][goals-agenda.org]] or related goals modules.
- Second priority: keep dissolving temporary staging files quickly whenever a real owning module already exists for their contents.
- Third priority: if the word-definition cluster grows enough to feel awkward in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]], consider a dedicated [[file+emacs:/Users/jay/emacs/emacs-settings/dictionary-lookup.org][dictionary-lookup.org]] rather than broadening the spellcheck module.
- Guiding rule: continue preferring existing boundaries before creating new files.

** [COMPLETED] 2026-04-19: Consolidate Org-roam-related residue into the existing suite
*Done (2026-04-19):*
- Renamed three misleading Org-roam headings in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] so the content matches the label.
- Cleaned stale Org-roam residue: deleted dead =#+begin_EXAMPLE= blocks (=org-refile-region-to-org-heading-or-roam-node=, =org-roam fail gracefully=), empty =* org-roam= section, redundant debug scan snippet, and the obsolete =jay/org-roam--use= / =jay/org-roam-use-main= / =jay/org-roam-use-life= multi-database helpers (superseded by [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-profiles.el][jay-org-roam-profiles.el]]; zero callers, path was outdated).
- Rewired =load-roam-config= to open the real suite core instead of the deprecation shim.
- Moved the Dropbox sync cache-reset code (=jay/org-cache-reset-roots= + helpers + =focus-in-hook=) into [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-core.el][jay-org-roam-core.el]]. This is the one true core-plumbing move.
- Moved =org-roam-ui=, =choose-refile-method-and-refile=, =select-refile-mode=, =org-roam-auto-link-buffer=, =kb/find-node-backlink=, and the four =jay/debug-*= parser-diagnostic commands into [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-extras.el][jay-org-roam-extras.el]], then wired that sidecar into [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]] immediately after the suite core.
- Verified that =s-'= still points at =choose-refile-method-and-refile= after the move.
- Net: ~210 lines removed from the monolith, one reliability helper correctly relocated, startup time halved (2.038s → 1.003s) though most of that gain came from earlier work.

** [PLANNED] Consolidate agenda/workflow residue into existing agenda modules
- Audit agenda, clocking, timeblocking, attach, and related workflow sections in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] for moves into [[file+emacs:/Users/jay/emacs/emacs-settings/goals-agenda.org][goals-agenda.org]] and related existing agenda/goals modules.
- Candidate sections in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]]: =* org-clock=, =* org-timesheet=, =* Eisenhower Matrix=, =* org-agenda=, =* org-attach=, =* archive tasks based on date=, =* org-timeblocking=.
- Reason: this is the right way to reduce workflow residue without creating a vague dumping-ground file like =org-workflow.org=.

** [COMPLETED] 2026-04-19: Create org-export.org after the consolidation pass
- Created [[file+emacs:/Users/jay/emacs/emacs-settings/org-export.org][org-export.org]] as the dedicated export/publishing module.
- Moved the live export/publishing cluster out of [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]]: Org export defaults, HTML/LaTeX/DOCX helpers, export filters, =ox-clip=, =ox-timeline=, =ox-json=, cover-page PDF export, ChatGPT export helpers, and export-only LaTeX glue.
- Wired [[file+emacs:/Users/jay/emacs/emacs-settings/org-export.org][org-export.org]] into [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]] before [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] for load-order safety.
- Intentionally left profile-owned =org-astro= behavior in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-profiles.el][jay-org-roam-suite/jay-org-roam-profiles.el]] rather than broadening the new module's scope.

* Completed Simplifications

** [COMPLETED] 2026-04-19: Consolidated Org-roam core plumbing (partial)
- See partial item above. First of multiple passes; sidecar creation still pending.

** [COMPLETED] 2026-04-19: Extracted spellcheck subsystem into spellcheck.org
- Created [[file+emacs:/Users/jay/emacs/emacs-settings/spellcheck.org][spellcheck.org]] as the dedicated home for Flyspell, Jinx, Hunspell, personal-dictionary wiring, and the large-buffer Flyspell guard.
- Moved all spellcheck clusters out of [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]]; verified zero remaining spellcheck references (only the separate =define-word= / =sdcv= word-definition subsystem still references "dictionary," and that's intentional).
- Wired into [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]] before =shared-functions.org= for load-order safety.
- *Savings:* one coherent subsystem extracted; spellcheck now editable as a focused unit.

** [COMPLETED] 2026-04-?: Extracted Org authoring helpers into org-authoring.org
- Created [[file+emacs:/Users/jay/emacs/emacs-settings/org-authoring.org][org-authoring.org]] for Org-only authoring helpers: heading insertion, list/checkbox predicates, Org-specific structure editing.
- Moved from [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.org][gnu-emacs-startup.org]] where they were miscategorized as "startup" code.

** [COMPLETED] 2026-04-?: Extracted prose editing into spacecraft-mode.org
- [[file+emacs:/Users/jay/emacs/emacs-settings/spacecraft-mode.org][spacecraft-mode.org]] now owns all prose-editing behavior (sentence movement, sentence killing, kill-word helpers, capitalization, smart punctuation).
- Cleanly separates "writing" concerns from "system bootstrap" concerns.

** [COMPLETED] 2026-04-?: Extracted search/completion into search-commands.org
- First extraction from the monolith; established the pattern that later extractions followed.

** [COMPLETED] 2025-10-26: Removed init-working.el backup
- Deleted =spacemacs.d/init-working.el= (old backup from Oct 14)
- Active config (=/Users/jay/emacs/emacs-settings/spacemacs.d/init.el=) is newer with better features
- Eliminated confusion between two similar files
- *Savings:* 1 redundant file removed

** [COMPLETED] 2025-10-26: Converted pasteboard to pure elisp
- Removed =pasteboard-copy-and-paste-functions.org=
- Source of truth is now =pasteboard-copy-and-paste-functions.el= (pure elisp)
- Added rich Commentary section with usage examples
- Removed auto-tangling overhead
- *Savings:* No more edit-tangle-test cycle, direct edit-test workflow

* High Priority (Active)

** [ACTIVE] Captain-mode fails to capitalize after "word space period"
Date: 2026-04-17

Captain-mode auto-capitalization doesn't fire when typing a period after a trailing space:
- "this." then typing → capitalizes next word (correct)
- "this " then "." then typing → does NOT capitalize next word (broken)

*Diagnostic steps (not yet done):*
1. Test with =C-q .= (literal period insert) after "word space" — if captain capitalizes, then =smart-period= is the culprit
2. Check =smart-period= implementation in [[file+emacs:/Users/jay/emacs/emacs-settings/spacecraft-mode.el][spacecraft-mode.el]] for how it inserts the period character
3. Check whether this is a pre-existing issue or was introduced by the 2026-04-17 defadvice migration (the migration only changed =capitalize-word= :after advice, which should be unrelated to captain auto-capitalization)

*Context:* Noticed during the defadvice-to-advice-add migration session. May be pre-existing — user was not certain it previously worked.

** [ACTIVE] Harden Pasteboard Regression Tests

*Why it matters*:
- Sanitizer regressions have bitten us multiple times (tables, emoji, headings)
- Manual testing is time-consuming and error-prone
- Don't catch issues until they hit production use

*Current state*:
- No automated tests
- Rely on manual smoke tests
- Regressions escape easily

*Proposal*:
1. Create =test-fixtures/= directory with sample inputs:
   - =markdown-with-tables.txt=
   - =emoji-compound.txt=
   - =mixed-markdown-org.txt=
   - =github-flavored-markdown.txt=
   - =chatgpt-output.txt=

2. Write batch Emacs script that:
   - Loads pasteboard functions
   - Runs =pasteboard--clean-string= on each fixture
   - Compares output to expected result
   - Reports differences

3. Add to git pre-commit hook or CI

*Example test*:
#+BEGIN_SRC emacs-lisp
;; test-pasteboard.el
(require 'ert)
(load-file "pasteboard-copy-and-paste-functions.el")

(ert-deftest test-markdown-table-separators ()
  "Table separators should be preserved"
  (let ((input "| Col1 | Col2 |\n|------|------|\n| A    | B    |")
        (expected "| Col1 | Col2 |\n|------|------|\n| A    | B    |"))
    (should (string= (pasteboard--clean-string input) expected))))
#+END_SRC

*Next steps*:
1. Collect problematic inputs from past bugs (see =codebase-wisdom.org=)
2. Create expected outputs
3. Write test harness
4. Run before each release

*Effort*: ~4-6 hours initial setup, ~30 minutes per new test

** [ACTIVE] Eliminate the Dual Boot Path

*Why it matters*:
Two complete startup systems create massive duplication:
- Spacemacs: =/Users/jay/emacs/emacs-settings/spacemacs.d/init.el= (1,255 lines) - ACTIVE
- GNU Emacs: =gnu-emacs-startup.el= (1,342 lines) - UNUSED

Settings must be synchronized across both, leading to drift and maintenance burden.

*Recommendation*:
1. Choose Spacemacs (currently active)
2. Delete =gnu-emacs-startup.el= and =gnu-emacs-startup.org=
3. Archive to git history if needed later

*Savings:* ~1,400 lines, eliminates dual-maintenance burden

*Implementation*:
#+begin_src bash
# Commit current state first
git add gnu-emacs-startup.{el,org}
git commit -m "Archive: GNU Emacs standalone config (moving to Spacemacs-only)"

# Remove files
rm gnu-emacs-startup.{el,org}
#+end_src

** [ACTIVE] Residue in spacemacs-new-config.el from old-era config

Small dead-code and stale-setting items found during the Apr 2026 review of [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]]. Low-risk cleanups — won't change behavior meaningfully, just reduces confusion.

Not the startup-time bottleneck — see the separate startup-loading item below.

Likely harmless cleanup:
- Lines 391-395: =(use-package wc-goal-mode :defer :ensure t :defer ...)= — =:defer= appears twice. Duplicate keyword, probably harmless but clearly wrong. Remove the second =:defer=.
- Lines 499-500: Commented =defadvice load-theme= — live version was migrated to =advice-add= in =gnu-emacs-startup.org= on 2026-04-17. The commented block is historical noise. Remove.
- Line 473: =(setq company-global-modes '(not org-mode))= — company is in =dotspacemacs-excluded-packages=, so this setting is inert. Remove.
- Line 556: =(setq global-auto-complete-mode -1)= — disables a feature we're removing entirely; becomes meaningless once =auto-complete= is removed from =init.el:191=. Remove only after that.
- Line 397: =(use-package counsel :defer)= — counsel is still in =init.el= additional packages at lines 105-107, so the setting is *not* dead today. Remove only as part of the counsel removal pass.

Not worth touching:
- Line 563: =(setq evil-emacs-state-cursor '("red"))= — Spacemacs depends on evil as a core package even though sub-packages like =evil-mode= are in the excluded list. This setting may still have effect in some spacemacs states. Leave alone unless you're doing a dedicated evil audit.

** [ACTIVE] Remove Commented/Dead Code

*Why it matters*:
Docs note "large blocks of commented-out code" throughout config.

*Examples found*:
- =spacemacs-new-config.el:23-36= (debugging code)
- Commented =use-package= blocks throughout
- Disabled layers in init files

*Implementation*:
#+begin_src bash
# Find commented use-package blocks
grep -n "^;; (use-package\|^;;  (load" *.el

# Find large commented blocks (10+ consecutive lines)
awk '/^;;/{n++} !/^;;/{if(n>10) print FILENAME":"NR-n"-"NR; n=0}' *.el

# After review, delete entire commented blocks
#+end_src

*Savings:* ~300-500 lines, improved readability

* Medium Priority (Planned)

** [COMPLETED] Create "Known Good" Recovery System

*Status*: Documented in =recovery-guide.org=

Simple manual approach using git tags + worktrees:
1. Tag working state: =git tag -f known-good HEAD=
2. Create worktree: =git worktree add ~/emacs/emacs-settings-known-good known-good=
3. When broken, launch with: =SPACEMACSDIR=~/emacs/emacs-settings-known-good/spacemacs.d emacs=

No automation needed - just manual git commands when things break.

** [PLANNED] Document Function Dependencies

*Why it matters*:
- Some functions depend on others being loaded first
- Loading order bugs are hard to debug
- Dependencies are implicit (no documentation)

*Current problem*:
- =key-minor-mode-map= must load before functions that use it
- =shared-functions.el= must load before modules that call its functions

*Proposal*:
Add dependency comments at top of each major file:

#+BEGIN_SRC emacs-lisp
;;; pasteboard-copy-and-paste-functions.el --- Clipboard integration
;;
;; Dependencies:
;;   - shared-functions.el (required)
;;   - jay-osx.el (optional, for key-minor-mode-map)
;;   - External: pbcopy, pbpaste (macOS command-line tools)
;;
;; Must load AFTER: shared-functions.el
;; Must load BEFORE: (none)
#+END_SRC

*Effort*: ~2-3 hours

** [PLANNED] Add Deprecation Tracking

*Why it matters*:
- Emacs and Org-mode deprecate functions regularly
- Warnings accumulate, eventually things break

*Proposal*:
1. Create =docs/deprecations.org= to track warnings
2. Run =emacs --batch --eval "(byte-compile-file \"file.el\")"= to find warnings
3. Fix high-priority before each major Emacs/Spacemacs upgrade

*Effort*: ~3-4 hours initial, ~1 hour per major update

** [PLANNED] Unify Keybinding Configuration

*Why it matters*:
Keybindings scattered across:
- =keys.el= (376 lines)
- =jay-osx.el= (initializes the shared =key-minor-mode-map=)
- =gnu-emacs-startup.el= (global-set-key calls)
- =shared-functions.org= (* Keybindings section)

*Recommendation:* Consolidate into =keys.el= only, organized by feature:
#+begin_src emacs-lisp
;;; keys.el --- All keybindings in one place -*- lexical-binding: t -*-

;; === Window Management ===
(global-set-key (kbd "s-1") 'delete-other-windows)

;; === Org Mode ===
(with-eval-after-load 'org
  (define-key org-mode-map (kbd "M-RET") 'org-meta-return))

(provide 'keys)
#+end_src

*Benefits:* Single source of truth for all keybindings

** [PLANNED] Consolidate Org-Mode Settings

*Why it matters*:
Org settings spread across:
- =/Users/jay/emacs/emacs-settings/spacemacs.d/init.el= (performance settings)
- =shared-functions.org= (org-mode settings)
- =org-visual-style.el=
- =org-yt.el=

*Recommendation:* Create unified =org-config.el= that loads all org modules.

** [PLANNED] Fix Structural Issues

*Problems identified*:
- Missing =lexical-binding= declarations
- Deprecated functions (=org-bracket-link-regexp= → =org-link-bracket-re=)
- Inconsistent quote styles
- Mixed indentation

*Implementation*:
#+begin_src bash
# 1. Find files missing lexical-binding
grep -L "lexical-binding: t" ~/emacs/emacs-settings/*.el

# 2. Add to each file (must be first line):
# -*- lexical-binding: t -*-

# 3. Find deprecated org variables
grep -r "org-bracket-link-regexp" ~/emacs/emacs-settings/
#+end_src

** [PLANNED] Rename misleadingly-named files

Several file names are actively misleading or stale, causing confusion when reasoning about module boundaries. Renames are low-effort and high-clarity but touch many cross-references (CLAUDE.md, design-architecture.org, spacemacs-new-config.el load paths, keybinding docs).

*Tier 1 — strong candidates:*

- [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.org][gnu-emacs-startup.org]] / [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.el][gnu-emacs-startup.el]] → =emacs-bootstrap.{org,el}= (or =editor-bootstrap=). Current name implies "standalone GNU Emacs startup, alternative to Spacemacs," but [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]] loads it unconditionally inside Spacemacs. The misnomer drove the need to invent explicit ownership rules in [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.md][CLAUDE.md]] ("should stay focused on startup/system/editor bootstrapping").
  - *Caveat*: conflicts with *[ACTIVE] Eliminate the Dual Boot Path* above, which proposes deleting this file entirely. Resolve that question first — if dual-boot is genuinely dead, delete; if still used standalone, rename.

- [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]] → =spacemacs-config.el= or =spacemacs-user-config.el=. "New" is stale — there is no "old" anymore. This is the main Spacemacs user-config loader.

*Tier 3 — leave alone:*

- =poetry_JD.el=, =fiverr-koma.el= — vendored/historical; renaming is risk without payoff.
- =shared-functions.org= — high-cost rename (many doc references). And the name is becoming accurate as it turns into the residue after coherent subsystems are extracted.

*Not a file, but worth checking:*
- [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.md][CLAUDE.md]] vs [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.org][CLAUDE.org]] — both exist. Claude Code itself reads =CLAUDE.md= specifically, so the =.md= is load-bearing. Worth verifying whether the =.org= is a duplicate or serves a distinct purpose.

*Implementation*: one rename per commit. Each commit updates cross-references (load paths, CLAUDE.md, keybinding docs, design-architecture.org) in the same commit so the repo stays internally consistent.

* Optional Improvements

** [OPTIONAL] Profile Startup Time

*Current*: Startup is ~5-8 seconds (acceptable but could be better)

*Proposal*:
#+BEGIN_SRC sh
emacs --debug-init --timed-requires
#+END_SRC

Identify top 5 slowest items, estimate potential gains, decide if worth optimizing.

*Status*: Startup is acceptable, optimization is premature

*Measure-first plan*:
1. Launch Emacs fresh and verify that [[file+emacs:/Users/jay/emacs/emacs-settings/docs/lab/startup-profile.org][docs/lab/startup-profile.org]] is actually being written by the startup timing hook in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]].
2. If the profile file is missing, debug that instrumentation before changing startup behavior.
3. Once the file exists, inspect the real slowest entries across a few launches instead of guessing.
4. Only then decide whether the biggest win is:
   - reducing =org-babel-load-file= usage at startup
   - removing or consolidating =gnu-emacs-startup.org=
   - deferring eager loads in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]]
5. Optimize the measured bottleneck first; avoid broad startup refactors without timing evidence.

*Current status*:
1. The timing writer is now working, and [[file+emacs:/Users/jay/emacs/emacs-settings/docs/lab/startup-profile.org][docs/lab/startup-profile.org]] exists.
2. The first real snapshot confirms that =org-babel-load shared-functions.org= and =org-babel-load search-commands.org= dominate the current profile.
3. A first audit of [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] found one true smoking-gun bug: an accidental unclosed =(dotimes (n 10)= that wrapped a long stretch of later top-level code and made it run 10 times in the tangled Elisp. That bug is now fixed.
4. The duplicate Projectile activation inside [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] has already been removed. Projectile still appears to be part of the live workflow, but its ownership now sits in [[file+emacs:/Users/jay/emacs/emacs-settings/search-commands.org][search-commands.org]]. Flycheck has already been narrowed from global activation to mode-scoped hooks for =prog-mode= and =conf-mode=, Smartparens now has a single startup owner, line numbers come on only in programming buffers, and the auto-revert policy is now consistent with the Dropbox-safe polling settings in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][spacemacs.d/init.el]].
5. The next startup optimization pass should begin with a fresh Emacs relaunch and a new look at [[file+emacs:/Users/jay/emacs/emacs-settings/docs/lab/startup-profile.org][docs/lab/startup-profile.org]], because the earlier =shared-functions.org= timing was distorted by that 10x wrap.

** [OPTIONAL] Convert Remaining Literate Org Files to Pure Elisp

*Current state*: 3 auto-tangling org files remain:
- =shared-functions.org= (324KB) - Keep as literate (too large/complex)
- =spacecraft-mode.org= (40KB) - Candidate for conversion
- =fonts-and-themes.org= (13KB) - Candidate for conversion

*Benefits*: Eliminates tangling errors, faster edit-test cycle

** [OPTIONAL] Use Package Manager Instead of Vendored Code

*Problem:* Large vendored files clutter the config:
- =dired+.el= (868KB)
- =highlight-tail.el= (48KB)
- =poetry_JD.el=, =forecast.el=, =ox-koma-letter.el=

*Recommendation*: Check MELPA availability or move to =vendor/= subdirectory.

** [OPTIONAL] Optimize Loading with use-package

*Problem:* Many packages loaded eagerly in =dotspacemacs-additional-packages=.

*Improved approach*:
#+begin_src emacs-lisp
(use-package org-transclusion
  :defer t
  :after org
  :commands org-transclusion-mode)
#+end_src

*Benefit:* Faster startup time

* Stretch Goals (Future)

** [IDEA] Package Critical Helpers as Reusable Modules

*What to package*:
- Pasteboard integration (as =pasteboard-mode=)
- Smart capitalization functions
- Org-roam profile switching (as =org-roam-profiles=)

*Effort:* Significant (~10-20 hours per package)

** [IDEA] Automated Linting

*Tools to consider*:
- =checkdoc= - Documentation string checker
- =package-lint= - Package metadata checker
- =elisp-lint= - General linting
- =relint= - Regexp linter

*Effort:* ~6-8 hours setup, ongoing maintenance

* Explicitly NOT Pursuing

** Multi-Platform Support
*Why not*: Primary platform is macOS. Pasteboard functions are macOS-specific. Cross-platform would dilute integration depth. Better to optimize for one platform.

** IDE-Like Features
*Why not*: This is a writing environment, not a development IDE. Focus is org-mode and writing.

** Org-Mode Alternatives
*Why not*: Deeply invested in org-mode. 10+ years of org-roam notes. Org-mode is the canonical format.

** Perfect Consistency
*Why not*: "Perfect is the enemy of good." Refactoring for aesthetics = busywork. Incremental improvement is fine.

*Philosophy*: Pragmatism over purity.

* Progress Tracking

| Change                         | Lines Saved  | Complexity Reduction | Status      |
|--------------------------------+--------------+----------------------+-------------|
| Remove init-working.el backup         | 1 file       | *                    | DONE        |
| Convert pasteboard to pure .el        | Workflow     | ***                  | DONE        |
| Extract search-commands.org           | Modular      | ***                  | DONE        |
| Extract spacecraft-mode.org           | Modular      | ****                 | DONE        |
| Extract org-authoring.org             | Modular      | ***                  | DONE        |
| Extract spellcheck.org                | Modular      | ***                  | DONE        |
| Consolidate Org-roam (core plumbing)  | ~210         | ***                  | PARTIAL     |
| Create jay-org-roam-extras.el sidecar | Modular      | **                   | Pending     |
| Consolidate agenda into goals-agenda  | Modular      | ***                  | Pending     |
| Create org-export.org                 | Modular      | ****                 | DONE        |
| Remove GNU Emacs dual boot            | ~1,400       | *****                | Pending     |
| Consolidate config loads              | ~200         | ***                  | Pending     |
| Remove commented code                 | ~300-500     | ****                 | Pending     |
| Fix deprecated functions              | ~50          | ** (stability++)     | Pending     |
| Unify keybindings                     | ~100         | ****                 | Pending     |
| Defer package loading                 | ~0           | **                   | Pending     |
| Consolidate org settings              | ~150         | ****                 | Pending     |
| Rename misleading files               | Clarity      | ***                  | Pending     |
|---------------------------------------+--------------+----------------------+-------------|
| *TOTAL*                               | *~2,200+*    | *Much easier*        | 7/17 done   |

* Decision Log

** 2025-12-31: Major documentation consolidation
*Reason*: Reduced docs/ from 14+ files to 11. Merged redundant files, extracted key lessons to codebase-wisdom.org, deleted obsolete incident logs.

** 2025-11-26: Merged documentation files
*Reason*: =critical-next-steps.org= and =suggested-next-steps.org= were redundant. Combined into single roadmap document.

** 2025-11-26: Completed Major Documentation Overhaul
*Reason*: Significant rewrite of core documentation files to improve clarity.

** 2025-10-26: Tabled "Document Hydras"
*Reason*: Solo user, hydras are discoverable enough, low ROI

** 2025-10-26: Prioritized Regression Tests
*Reason*: Clipboard bugs have cost hours of debugging multiple times

** 2025-10-26: Decided NOT to Pursue Multi-Platform
*Reason*: macOS integration is a core value, would dilute focus

* Quick Wins You Can Do Right Now

#+begin_src bash
# 1. Count commented code
grep -c "^;; (" ~/emacs/emacs-settings/*.el | grep -v ":0$"

# 2. Find files missing lexical-binding
for f in ~/emacs/emacs-settings/*.el; do
  grep -q "lexical-binding: t" "$f" || echo "$f"
done

# 3. Profile current startup time
emacs --eval '(message "Startup time: %s" (emacs-init-time))' --kill
#+end_src

* Monthly Review Checklist

- [ ] Are high-priority items progressing?
- [ ] Any new critical issues to add?
- [ ] Can anything move from active → planned → done?
- [ ] Are stretch goals still relevant?

* Related Documentation

- =work-log.org= - History of what's been done
- =codebase-wisdom.org= - Lessons from bugs
- =design-architecture.org= - Current architecture
- =the-emacs-settings-approach.org= - Philosophy and principles
