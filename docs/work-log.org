#+TITLE: Emacs Configuration Work Log
#+STARTUP: showall

* 2026-04-17: Wire Jinx into existing personal word dictionary
- Jinx uses =enchant=, which on this macOS system is backed by =aspell= (not hunspell — Homebrew's enchant build has no hunspell plugin).
- Existing personal word list at =/Users/jay/emacs/Spelling/personal.dic= (4057 words, plain format) was not being read by enchant.
- Created =~/.config/enchant/enchant.ordering= with =en_US:hunspell,aspell= for future hunspell support (no-op for now, harmless).
- Symlinked =~/.config/enchant/en_US.dic -> /Users/jay/emacs/Spelling/personal.dic= so enchant reads the existing personal word list directly.
- Added startup automation in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] so Jinx re-creates that Enchant bridge automatically on machines or profiles where it is missing.
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/design-architecture.org][docs/design-architecture.org]], [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]], and the keybinding docs so they reflect the live Jinx + Enchant setup and the backtick Jinx correction command.
- Jinx now recognizes personal words (e.g. "Jay", "Dixit"). New words added via Jinx will write back to =personal.dic= through the symlink, keeping a single source of truth.

* 2026-04-17: Refresh modernization plan after successful runtime checkpoints
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/completion-modernization-plan.org][docs/completion-modernization-plan.org]] so it reflects the current confirmed state rather than the intermediate audit state.
- Marked the completed work more clearly: key-chord modernization, =ivy-smex= startup removal, stale =smex= cleanup, direct =ivy-read= helper modernization, proposal/book path fixes, and live =jinx= + backtick correction workflow.
- Corrected the older audit sections so they no longer imply that =helm-imenu= / =helm-M-x= bindings, direct =ivy-read= helpers, or =swiper= key-chords are still the current live state.

* 2026-04-17: Migrate defadvice to modern advice-add
- Converted 3 active =defadvice= calls to =advice-add= across tracked source files:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.org][gnu-emacs-startup.org]]: =load-theme= =:before= advice to disable active themes before loading a new one
  - [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][spacemacs.d/init.el]]: =file-notify-add-watch= =:around= advice to suppress file-notify errors
  - [[file+emacs:/Users/jay/emacs/emacs-settings/spacecraft-mode.org][spacecraft-mode.org]]: =capitalize-word= =:after= advice to downcase the next word after capitalizing
- Removed duplicate dead =defadvice= on =capitalize-word= in spacecraft-mode (first copy was overwritten by second)

* 2026-04-17: Rewrite backtick auto-correct to use Jinx internals
- Rewrote =jay/jinx-correct-word-near-point= in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] to auto-apply the first Jinx suggestion instead of opening a minibuffer chooser.
- Uses =jinx--bounds-of-word= for accurate word detection and =jinx--force-overlays= to ensure the word has been spell-checked before correction.
- Preserves the old backtick ergonomics: one press auto-corrects, steps back over trailing space if needed.
- Backtick binding in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] unchanged (still =jay/jinx-correct-word-near-point=).
- Old Flyspell helper kept but no longer used.

* 2026-04-17: Move backtick correction key from Flyspell helper to Jinx (initial)
- Initial version that called =jinx-correct-word= (minibuffer chooser) — replaced by rewrite above.

* 2026-04-17: Make jinx live
- Added =jinx= to [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][spacemacs.d/init.el]] so the package is actually installed in the Emacs config.
- Converted the =jinx= snippet in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] from an example block into live code so =jinx-mode= is enabled for text, org, and conf buffers.
- This follows the earlier runtime test: =embark= already worked at runtime, but =jinx-mode= did not exist yet.

* 2026-04-17: Fix stale proposal/book search paths
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/search-commands.org][search-commands.org]] so the proposal/book search helpers no longer hardcode the old missing directories =/Users/jay/Dropbox/writing/proposal= and =/Users/jay/Dropbox/writing/book=.
- Added path-resolution helpers that prefer the live directories [[file+emacs:/Users/jay/Dropbox/writing/both/proposal][/Users/jay/Dropbox/writing/both/proposal]] and [[file+emacs:/Users/jay/Dropbox/writing/both/book][/Users/jay/Dropbox/writing/both/book]], while still checking CloudStorage and older fallback paths.
- This fixes the runtime failure in =search-filename-both-book-and-proposal-directories= and also updates the single-directory and ripgrep helpers to use the resolved paths.

* 2026-04-17: Modernize remaining direct ivy-read helpers
- Replaced the remaining direct =ivy-read= usage in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] and [[file+emacs:/Users/jay/emacs/emacs-settings/search-commands.org][search-commands.org]] with plain =completing-read= so those commands now use the current minibuffer completion UI naturally.
- Updated =bjm/ivy-dired-recent-dirs= to use =completing-read= while keeping the command name stable for existing bindings.
- Updated =counsel-fzf-both-proposal-and-book-dirs= to use =completing-read= for the final file selection step.
- Removed the now-unnecessary explicit =use-package ivy= / =:after ivy= coupling from the =fasd= block in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]].

* 2026-04-17: Tighten CLAUDE guidance on post-checkpoint test scope
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.org][CLAUDE.org]] / [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.md][CLAUDE.md]] to require narrowly scoped post-checkpoint test requests.
- Added explicit rules to ask only for startup checks and tests of behaviors actually changed by the diff, avoid broadening test scope without cause, explain any non-obvious test request, and skip Emacs relaunches for docs-only checkpoints.

* 2026-04-17: Remove stale smex residue after ivy-smex cleanup
- Removed the old =smex-save-file= custom variable from [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][spacemacs.d/init.el]].
- Removed stale =smex=-specific leftovers from [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]], including the unused =smex-prompt-string= setting and the no-op minibuffer abbrev guard that only looked for =smex-= commands.
- Kept the generic minibuffer abbrev disable/restore behavior in place.

* 2026-04-17: Stop loading ivy-smex at startup
- Removed the [[file+emacs:/Users/jay/emacs/emacs-settings/ivy-smex.el][ivy-smex.el]] load from [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]] after Jay confirmed he does not care about distinct Smex behavior.
- Left =amx= in place as the live command-palette path.
- Recorded the new state in [[file+emacs:/Users/jay/emacs/emacs-settings/docs/completion-modernization-plan.org][docs/completion-modernization-plan.org]] and left follow-up cleanup of residual =smex= variables for later.

* 2026-04-17: Switch SS and BB to Consult search commands
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/prelude-key-chord.el][prelude-key-chord.el]] so =SS= now runs =consult-line= instead of =swiper=.
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/prelude-key-chord.el][prelude-key-chord.el]] so =BB= now runs =consult-line-multi= instead of =swiper-all=.
- This is a deliberate workflow experiment to test whether a more unified Consult-based search flow feels better in real use than the remaining Ivy/Swiper bindings.

* 2026-04-17: Make DD recent-directories command live
- Converted =bjm/ivy-dired-recent-dirs= from documentation/example text into live code in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]].
- Root cause: the function existed only inside an example block, so it never tangled into [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.el][shared-functions.el]] even though [[file+emacs:/Users/jay/emacs/emacs-settings/prelude-key-chord.el][prelude-key-chord.el]] bound =DD= to it.
- Hardened the command by ensuring =recentf= is available, enabling =recentf-mode= if needed, skipping nil directory entries, and showing a clear error when there are no recent directories.

* 2026-04-17: Add ivy/swiper vs consult audit to modernization plan
- Added a dedicated Ivy / Swiper vs Consult audit to [[file+emacs:/Users/jay/emacs/emacs-settings/docs/completion-modernization-plan.org][docs/completion-modernization-plan.org]].
- Main finding: the current state is a genuinely mixed stack, not a cleanly completed migration. Ivy / Swiper still powers live muscle-memory bindings like =DD=, =SS=, and =BB=, while Consult already powers other active bindings and search helpers.
- Recorded the safe conclusion: do not remove Ivy / Swiper yet; first have Jay test whether those exact live behaviors are still preferred before migrating them.

* 2026-04-17: Add amx vs ivy-smex audit to modernization plan
- Added a dedicated =amx= vs =ivy-smex= audit to [[file+emacs:/Users/jay/emacs/emacs-settings/docs/completion-modernization-plan.org][docs/completion-modernization-plan.org]].
- Main finding: =amx= is now the clearly live command-palette surface (direct bindings in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] and [[file+emacs:/Users/jay/emacs/emacs-settings/prelude-key-chord.el][prelude-key-chord.el]]), while [[file+emacs:/Users/jay/emacs/emacs-settings/ivy-smex.el][ivy-smex.el]] still loads but looks more like a future cleanup candidate than a first-class preferred path.
- Recorded the safe conclusion: do not remove =ivy-smex.el= yet, but treat it as a likely later cleanup once Jay confirms no distinct Smex behavior is still wanted.

* 2026-04-17: Replace remaining Helm key-chords with modern commands
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/prelude-key-chord.el][prelude-key-chord.el]] so =KK= now runs =consult-imenu= instead of =helm-imenu=.
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/prelude-key-chord.el][prelude-key-chord.el]] so =XX= now runs =amx= instead of =helm-M-x=.
- Rationale: both bindings were part of the live Helm overlap. These replacements preserve the same rough workflows (jump to headings, command palette) while aligning better with the current completion stack.
- Left [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.md][CLAUDE.md]] / [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.org][CLAUDE.org]] exactly as currently arranged per Jay's instruction.

* 2026-04-17: Strengthen CLAUDE guidance against runtime speculation
- Expanded the AI editing guidance in [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.org][CLAUDE.org]] / [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.md][CLAUDE.md]] to make runtime-testing expectations explicit.
- Added rules to distinguish static config analysis from actual runtime behavior, forbid unsupported "probably broken" claims, and require a direct Jay test for uncertain keybinding/function behavior.

* 2026-04-17: Add helm conflict audit to modernization plan
- Added a dedicated =helm= conflict audit to [[file+emacs:/Users/jay/emacs/emacs-settings/docs/completion-modernization-plan.org][docs/completion-modernization-plan.org]].
- Main finding: [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][spacemacs.d/init.el]] excludes =helm=, but [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/jay/packages.el][spacemacs.d/jay/packages.el]] and [[file+emacs:/Users/jay/emacs/emacs-settings/prelude-key-chord.el][prelude-key-chord.el]] still rely on Helm commands and bindings.
- Conclusion recorded for future work: do not remove Helm yet; first decide whether =helm-imenu= and =helm-M-x= are still preferred or should be replaced with modern equivalents.

* 2026-04-17: Record completion overlap audit in modernization plan
- Added an audit snapshot to [[file+emacs:/Users/jay/emacs/emacs-settings/docs/completion-modernization-plan.org][docs/completion-modernization-plan.org]] summarizing which completion-era packages are clearly still live, which ones overlap, and which ones look like likely future cleanup candidates.
- Important result: =counsel= family, =key-chord=, =helm-make=, =amx=, =affe=, and =fasd= all still have live workflow footprints, so they should not be treated as automatic cleanup targets.
- Also recorded that the main current overlap area is the mixed =compleseus= plus =ivy/swiper/smex/helm= residue rather than a single obsolete package.

* 2026-04-17: Add checkpoint protocol to CLAUDE docs
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.org][CLAUDE.org]] (symlinked to [[file+emacs:/Users/jay/emacs/emacs-settings/CLAUDE.md][CLAUDE.md]]) to record the checkpoint protocol for future editing sessions.
- The documented protocol is: update [[file+emacs:/Users/jay/emacs/emacs-settings/docs/work-log.org][docs/work-log.org]], commit the checkpoint, have Jay relaunch Emacs and confirm clean startup, and only then continue.

* 2026-04-17: Switch icons to nerd-icons and add modernization plan
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][spacemacs.d/init.el]] to use =dotspacemacs-default-icons-font 'nerd-icons= instead of =all-the-icons=.
- Added [[file+emacs:/Users/jay/emacs/emacs-settings/docs/completion-modernization-plan.org][docs/completion-modernization-plan.org]] to capture deferred cleanup work for the completion stack instead of removing packages ad hoc.
- Explicitly kept current user-preferred tools for now: =counsel= family, =ctrlf=, =key-chord=, and the existing compile-prefix workflow that still depends on =helm-make=.

* 2026-04-17: Remove stray Astro Eglot notes from shared-functions.org
- Cleaned up the =* Astro= section in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] by deleting old Eglot-specific notes and code that were sitting outside a source block.
- Rationale: this setup does not want Eglot for Astro, and the text was misleading because it looked like live config even though it did not tangle into [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.el][shared-functions.el]].
- Kept the actual live Astro setup minimal: =web-mode= foundation, =astro-mode= derivation, =auto-mode-alist= entry, and the =revert-without-query= tweak for =.mdx= files.

* 2026-04-17: Freeze helm-make to avoid fragile Quelpa fork updates
- Investigated a package-update failure that looked like it might live in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][/Users/jay/emacs/emacs-settings/shared-functions.org]], but the real recipe lives in Spacemacs itself at [[file+emacs:/Users/jay/emacs/spacemacs/layers/+completion/compleseus/packages.el][/Users/jay/emacs/spacemacs/layers/+completion/compleseus/packages.el]].
- Root cause: the =compleseus= layer pulls =helm-make= from the fork [[file+emacs:https://github.com/myrgy/helm-make][https://github.com/myrgy/helm-make]] on branch =add_emacs_completion= because upstream =abo-abo/helm-make= still lacks the =emacs= completion method that this setup enables.
- Verified the fork and branch still exist and that the exact clone command succeeds from the shell, which means the failure is best treated as an update-time fragility around a forked Quelpa recipe rather than a missing repository.
- Chose the least invasive fix in user config: froze =helm-make= in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][/Users/jay/emacs/emacs-settings/spacemacs.d/init.el]] so Spacemacs no longer tries to update that package during bulk package refreshes.
- Rationale: =helm-make= is already installed locally, the special fork is only needed to preserve =helm-make-completion-method 'emacs=, and freezing avoids repeated Quelpa checkout failures without patching Spacemacs vendor code.

* 2026-04-17: Repair stale Homebrew Emacs cask warning and shell PATH init
- Investigated a Homebrew warning about invalid cask metadata under [[file+emacs:/opt/homebrew/Caskroom/emacs-mac-spacemacs-icon][/opt/homebrew/Caskroom/emacs-mac-spacemacs-icon]] while the real installed Emacs cask was [[file+emacs:/opt/homebrew/Caskroom/emacs-mac][/opt/homebrew/Caskroom/emacs-mac]].
- Verified the live app and binary were intact at [[file+emacs:/Applications/Emacs.app][/Applications/Emacs.app]] and [[file+emacs:/opt/homebrew/bin/emacs][/opt/homebrew/bin/emacs]].
- Removed the stale caskroom metadata directory after confirming it was only leftover metadata, not the active installation.
- Confirmed the reinstall path Homebrew suggested was not appropriate here because =emacs-mac-spacemacs-icon= conflicted with the already-installed =emacs-mac= cask.
- Traced the remaining =brew doctor= PATH warning to shell startup config: [[file+emacs:/Users/jay/.bash_profile][/Users/jay/.bash_profile]] loaded Homebrew only for interactive shells via [[file+emacs:/Users/jay/Dropbox/apps/bash-profile/bashrc-master.sh][/Users/jay/Dropbox/apps/bash-profile/bashrc-master.sh]], leaving non-interactive login shells with =/usr/bin= ahead of =/opt/homebrew/bin=.
- Fixed the root cause in the bash profile repo by extracting shared PATH setup into [[file+emacs:/Users/jay/Dropbox/apps/bash-profile/shared-path-init.sh][/Users/jay/Dropbox/apps/bash-profile/shared-path-init.sh]] and sourcing it from both [[file+emacs:/Users/jay/.bash_profile][/Users/jay/.bash_profile]] and [[file+emacs:/Users/jay/Dropbox/apps/bash-profile/bashrc-master.sh][/Users/jay/Dropbox/apps/bash-profile/bashrc-master.sh]].
- Verified in fresh shells that =brew=, =jq=, and =python3= now resolve to =/opt/homebrew/bin= and that =brew doctor= reports a clean setup.

* 2026-04-06: Install org-mindmap and add live mindmap examples
- Added =org-mindmap= to =package-vc-selected-packages= in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][spacemacs.d/init.el]] so the package is tracked in the same VC-install path as =org-srs= and =lisp-fsrs=.
- Added an =org mindmaps= section to [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] with:
  - a =use-package= stanza for =org-mindmap=
  - live =#+begin_mindmap= examples for list conversion and the three layouts
  - a larger interactive example adapted from the upstream README
- Avoided =:vc= inside =use-package=: this setup is still on the older ELPA =use-package= build, which does not understand the =:vc= keyword.
- Pointed the package explicitly at =/Users/jay/.emacs.d/elpa/org-mindmap= via =:load-path= so the running Spacemacs session can load the VC-installed checkout reliably.
- Verified in the live Emacs server:
  - =(locate-library "org-mindmap")= resolves to =/Users/jay/.emacs.d/elpa/org-mindmap/org-mindmap.elc=
  - =(featurep 'org-mindmap)= returns =t=
- Retangled [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] and ran =check-parens= on [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.el][shared-functions.el]].

* 2026-04-08: Improve visit-most-recent-file behavior
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] and [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.el][shared-functions.el]] to make =visit-most-recent-file= more reliable.
- Changed the command to open the true most-recent unvisited local regular file with resilient path normalization.
- Added fallback completion when no auto-match exists, with compact recent entries shown as `#1 — parent/filename`, `#2 — ...`, etc.
- Hardened selection by skipping remote, non-regular, and missing files and by avoiding stale/invalid `recentf` entries.

* 2026-04-01: Fix whittle-transcript trailing filler rule eating paragraph breaks
- Fixed =whittle/transcript-edge-filler-rules= in [[file+emacs:/Users/jay/emacs/emacs-settings/whittle.el][whittle.el]] so the sentence-final filler cleanup no longer uses =[[:space:]]*$=.
- Root cause: =[[:space:]]= includes newlines, so removing trailing =okay/right/you know= at end of a line could also consume the blank line before the next paragraph or =*** Speaker:= heading.
- Changed the rule to =[[:blank:]]*= so it only trims spaces/tabs and preserves real line structure in Org transcripts.
- Tightened the rule further so sentence-final =okay= is no longer removed at all; phrases like =Is everything okay?= are meaningful content, not filler.
- Added word boundaries around transcript edge fillers so sentence-start =so= no longer clips the first letters off real words like =Sometimes=.

* 2026-03-24: LaTeX template refinements
- Suppressed header/footer on first page across all document templates (10 files) via =\thispagestyle{empty}= in =\AtBeginDocument=
  - Templates updated: elegant-garamond, elegant-garamond-tabularray, elegant-helvetica, elegant-helvetica-tabularray, elegant-less-whitespace, beautiful-racket, beautiful-racket-tabularray, beautiful-racket-classic, new-yorker, resonate
  - Skipped book-proposal-latex and modest-ruin (already had it in =\maketitle=)
- Reduced subtitle font size in elegant-garamond from 28pt to 14pt for a more understated look
- Reduced subsubsection top spacing in elegant-garamond from 16pt to 6pt

* 2026-03-19: Fix whittle punctuation cleanup destroying newlines and capitalization
- Fixed =whittle--cleanup-punctuation= using =[[:space:]]= (which includes newlines) instead of =[[:blank:]]= (space/tab only) in two regexps:
  - "strip whitespace before punctuation" was collapsing =:PROPERTIES:= drawers onto one line
  - "collapse multiple spaces" was merging paragraphs by replacing =\n\n= with a single space
- Added =whittle--normalize-case= call to the conservative =whittle= command — previously only =whittle-transcript= fixed capitalization after filler removal
- Added post-heading capitalization rule to =whittle--normalize-case= so text after org headings (e.g. =*** Speaker:=) gets capitalized when a leading filler like =Um,= is removed
- Fixed =, like,= filler rule eating both commas — =bias, like, oh= became =bias oh= instead of =bias, oh=; replacement changed from =" "= to =", "=

* 2026-03-17: Split whittle into conservative cleanup and transcript mode
- Reworked [[file+emacs:/Users/jay/emacs/emacs-settings/whittle.el][whittle.el]] so =whittle= stays conservative and a new =whittle-transcript= command handles aggressive transcript cleanup.
- Fixed the original command-path bug where =M-x whittle= errored with no active region; both commands now work on region or whole buffer.
- Improved cleanup behavior:
  - filler-chain removal for transcript junk like =um, uh, you know,=
  - sentence-edge filler removal for words like =well=, =right=, =you know=, and sentence-start =Like,=
  - smarter duplicate and false-start collapsing such as =I I I think= and =we were we were ready=
  - stronger punctuation cleanup for stray commas, space-before-punctuation, and ellipsis normalization
  - transcript line joining and sentence/lone-=i= capitalization
  - preserved intentional repeats like =go go= and =bye bye=
- Updated docs:
  - Added a new “Transcript Cleanup” section to [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]]

* 2026-03-14: Fix org export hook crash from replace-regexp
- Replaced the anonymous =org-export-before-parsing-functions= lambda in =shared-functions.org= that emphasized leading =Label:= prefixes.
- Root cause: the hook used buffer-level =replace-regexp= during export, which calls =replace-dehighlight= and can crash inside Magit's =isearch-clean-overlays= advice when a stale overlay is present.
- Added named helper =jay/org-export-emphasize-leading-labels= that uses =re-search-forward= + =replace-match= instead of interactive replacement machinery.
- Kept the same behavior: emphasize line-start labels like =Question:= while still avoiding URL schemes such as =https:=.
- Updated docs:
  - Added a prevention note to =docs/codebase-wisdom.org=

* 2026-03-06: Clean up org-roam profile templates
- Merged default profile's two template lists (=jay/org-roam-default-template-specs= and =jay/org-roam-default-factory-template-specs=) into a single alphabetically sorted list (A a B b C c...)
- Simplified parents profile: replaced 6 over-engineered templates (care log, medical note, parent dossier, etc.) with 7 standard ones (conversation, document, interview, note, person, story, task)
- All profiles now use clean =jay/roam-template= helper with minimal bodies

* 2026-03-06: Fix smart-return to follow plain URLs
- =org-link-at-point-p= previously only matched bracket-style links (=[​[...]​]=)
- Added =org-link-plain-re= check so bare URLs (e.g., =https://example.com/path=) are also recognized
- Smart-return now opens plain URLs on RET, consistent with bracket link behavior

* 2026-03-04: Restyle org-code face to GitHub-style inline code
- Replaced old org-code face (green text, grey box, shadow inherit) with cleaner GitHub-inspired style
- New face: dark red =#a0222b= on light gray =#f6f8fa= background, Monaco font at 0.85 height
- Removed =:box= and =:inherit shadow= for a cleaner, less cluttered look

* 2026-03-03: Update goals documentation to reflect new agenda behavior
- Updated [[file+emacs:/Users/jay/Dropbox/roam/velocity/goals-system/goals-instructions-for-Claude.org][goals-instructions-for-Claude.org]]:
  - Corrected "Jay does NOT use org-agenda" → org-agenda is optional but configured and working (=s-k a d=).
  - Marked energy tags (#focus, #admin, etc.) as optional organizational sugar throughout — plain =TODO= headings are all that's needed for agenda visibility.
  - Clarified that =TODO= state is the required element, not tags.
  - Updated task delivery section: journal is the guaranteed path, but SCHEDULED timestamps also appear in org-agenda if Jay opens it.

* 2026-03-03: Replace tag-driven agenda with dynamic TODO dashboard (while preserving existing agenda files)
- Updated agenda config in:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/goals-agenda.org][goals-agenda.org]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/goals-agenda.el][goals-agenda.el]]
- Replaced static/stale agenda source assumptions with dynamic discovery:
  - journal files from =~/Dropbox/roam/journal/=
  - project files from =~/Dropbox/roam/velocity/projects/=
  - lead files from =~/Dropbox/roam/lead-tracking/=
- Preserved all existing agenda sources:
  - captured prior =org-agenda-files= as baseline
  - merged baseline + dynamic files instead of replacing
  - deduped and filtered unreadable/missing paths
- Fixed agenda refresh timing:
  - moved refresh to before agenda build via =advice-add= on =org-agenda=
  - guarded advice with =advice-member-p= to prevent duplicate advice on re-eval
- Simplified the =d= custom view:
  - =TODAY= block (scheduled today)
  - =ALL OPEN TASKS= block (all TODOs, file/category-prefixed)
  - no dependency on =#focus/=#admin/=#connect/=#batch= tags
- Improved journal carry-forward behavior:
  - now uses today's daily note (if present) + previous 3 existing dated journal files
  - fallback: latest 4 existing dated journal files when today is missing
- Validation:
  - extracted elisp block to =goals-agenda.el=
  - ran =check-parens=
  - batch-loaded agenda config and confirmed dynamic file list populated

* 2026-03-02: Fix org-agenda layout breakage + simplify daily view readability
- Symptom: Daily agenda view was visually broken (wrapped/ghost lines, tag spillover, hard-to-scan long prefixes).
- Updated agenda config in:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/goals-agenda.org][goals-agenda.org]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/goals-agenda.el][goals-agenda.el]]
- Changes made:
  - Added =jay/org-agenda-make-it-not-deranged= to force stable agenda rendering:
    - disable visual-line wrapping
    - enable truncate-lines
    - force fixed-pitch face in agenda buffer
    - set local tags column for right-side stability
  - Hooked it on both =org-agenda-mode-hook= and =org-agenda-finalize-hook= so redraws keep the same formatting.
  - Simplified =org-agenda-prefix-format= to reduce left-column overflow.
  - Enabled =org-agenda-remove-tags= to eliminate tag spill lines.
- One-time operational cleanup performed in live Emacs session:
  - Rescheduled 7 recurring items (daily/weekly repeaters) from 2024–2025 dates to today across:
    - [[file+emacs:/Users/jay/Dropbox/roam/velocity/projects/substack-launch.org][substack-launch.org]]
    - [[file+emacs:/Users/jay/Dropbox/roam/velocity/projects/daily-writing.org][daily-writing.org]]
    - [[file+emacs:/Users/jay/Dropbox/roam/velocity/projects/video-content-strategy.org][video-content-strategy.org]]
    - [[file+emacs:/Users/jay/Dropbox/roam/velocity/projects/revenue-generation-q4.org][revenue-generation-q4.org]]
    - [[file+emacs:/Users/jay/Dropbox/roam/velocity/projects/socratic-ai-execution.org][socratic-ai-execution.org]]
    - [[file+emacs:/Users/jay/Dropbox/roam/velocity/goals/20250519215301-master-task-list.org][master-task-list.org]]
  - Marked 4 completed one-time items as DONE (welcome post, welcome email, Dan Ariely email, YouTube studio).
  - Rescheduled "Create proposal template" to today (still needed).
  - ~60 additional stale one-time TODOs remain across project files — triage deferred.
- Commit:
  - =48e63835= — =fix(agenda): stabilize layout and simplify prefixes=

* 2026-03-02: Fix org-roam-dailies navigation — stray daily note at roam root
- Found that =s-u p= (=org-roam-dailies-goto-previous-note=) was navigating to a random org-roam file instead of the previous daily note.
- Root cause: A stray =2026-03-02.org= existed at =~/Dropbox/roam/= (the roam root) instead of in =~/Dropbox/roam/journal/= where dailies are configured to live (=org-roam-dailies-directory= is ="journal/"=).
- The stray file was an 11-line stub with a different ID; the real daily note (287 lines) was already in =journal/=.
- Removed the stray stub.
- Added instruction to [[file+emacs:/Users/jay/Dropbox/github/writing-trackers/prolific/scripts/cli/morning-claude-instructions.md][morning-claude-instructions.md]] that daily notes must always go in the =journal/= subdirectory.

* 2026-02-26: Parity pass for tabularray/classic xcolor + tabularray bookmarks
- Completed remaining xcolor option-loading fixes in templates still using the no-op pattern:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-helvetica-tabularray.el][latex-templates/elegant-helvetica-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-tabularray.el][latex-templates/beautiful-racket-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-classic.el][latex-templates/beautiful-racket-classic.el]]
- Change made:
  - Replaced =\\usepackage{xcolor}= + =\\PassOptionsToPackage{hyperref,x11names}{xcolor}= with =\\usepackage[x11names]{xcolor}=.
- Added =bookmark= package (after hyperref) to all tabularray proposal classes:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-helvetica-tabularray.el][latex-templates/elegant-helvetica-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond-tabularray.el][latex-templates/elegant-garamond-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-tabularray.el][latex-templates/beautiful-racket-tabularray.el]]
- Bookmark defaults used:
  - =\\bookmarksetup{numbered=false,open=true}=

* 2026-02-26: Add bookmark package, fix xcolor bug, remove dead onehalfspacing
- Added =bookmark= package (loaded after hyperref) to all three core templates:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant.el][latex-templates/elegant.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond.el][latex-templates/elegant-garamond.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket.el][latex-templates/beautiful-racket.el]]
- Fixes PDF bookmark ordering issues that hyperref alone can have.
- Fixed xcolor =\PassOptionsToPackage= bug in elegant.el and beautiful-racket.el:
  - Replaced no-op =\usepackage{xcolor}= + =\PassOptionsToPackage{hyperref,x11names}{xcolor}= with =\usepackage[x11names]{xcolor}=.
  - The =\PassOptionsToPackage= call after =\usepackage= does nothing — options must be passed before loading.
- Removed dead =\onehalfspacing= from beautiful-racket.el and elegant-garamond.el:
  - =\setstretch{1.2}= on the next line was already overriding it completely.

* 2026-02-26: Export src verse/poetry as real LaTeX verse blocks
- Added shared helper:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/jay-latex-poetry-blocks.el][latex-templates/jay-latex-poetry-blocks.el]]
- Behavior:
  - For LaTeX exports, =#+begin_src verse= and =#+begin_src poetry= are rewritten to Org =VERSE= blocks before parsing.
  - Resulting LaTeX uses proper =\\begin{verse}...\\end{verse}= output.
  - Other src languages remain unchanged and continue exporting as code/verbatim blocks.
- Wired helper loading into proposal class files:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant.el][latex-templates/elegant.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond.el][latex-templates/elegant-garamond.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket.el][latex-templates/beautiful-racket.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-helvetica-tabularray.el][latex-templates/elegant-helvetica-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond-tabularray.el][latex-templates/elegant-garamond-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-tabularray.el][latex-templates/beautiful-racket-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-classic.el][latex-templates/beautiful-racket-classic.el]]
- Updated usage docs:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]]
  - ~/Dropbox/roam/vibecoding/20260225235900-latex_org_mode_documentation.org

* 2026-02-26: Add opt-in enotez endnotes toggle across proposal classes
- Replaced/standardized endnote behavior across:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant.el][latex-templates/elegant.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond.el][latex-templates/elegant-garamond.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket.el][latex-templates/beautiful-racket.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-helvetica-tabularray.el][latex-templates/elegant-helvetica-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond-tabularray.el][latex-templates/elegant-garamond-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-tabularray.el][latex-templates/beautiful-racket-tabularray.el]]
- New shared toggle pattern in each class:
  - ~\\usepackage{enotez}~
  - ~\\EnableEndnotes~ / ~\\DisableEndnotes~
  - default remains normal footnotes unless explicitly enabled
  - when enabled, ~\\footnote~ is remapped to ~\\endnote~ at begin-document
- Updated user docs:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]] (new “Common LaTeX Toggles” section)
  - Added matching notes in roaming doc: ~/Dropbox/roam/vibecoding/20260225235900-latex_org_mode_documentation.org

* 2026-02-26: Document tabularray positioning (visual parity + opt-in guidance)
- Updated docs to clarify expected outcomes of the tabularray migration:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/latex-table-export-trace.org][docs/latex-table-export-trace.org]]
- Documented decision framing:
  - Side-by-side output is expected to look very similar for normal proposal tables.
  - The value of tabularray is primarily internal robustness/maintainability, not visible redesign.
  - Classic classes remain default production choice; tabularray classes are an opt-in path for table edge cases.

* 2026-02-26: Tabularray regression fix pass (agenda tables)
- Follow-up fixes to the new tabularray path after real-document testing exposed two issues:
  - 3-hour agenda looked blank in the first side-by-side comparison (page-mapping mismatch in comparison artifacts).
  - 2-hour agenda overflowed into footer in early conversion behavior.
- Updated shared converter [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/jay-latex-table-tabularray.el][latex-templates/jay-latex-table-tabularray.el]]:
  - Added table font normalization in converted tables: ~rows={font=\\fontsize{10}{12}\\selectfont}~.
  - Added wrapper cleanup to strip Org’s surrounding ~center~ environment around converted tabularray blocks.
  - Normalized conversion to ~tblr~ for Org-exported ~tabular~, ~tabularx~, and ~longtable~ output (instead of emitting ~longtblr~ for agenda tables).
- Updated tabularray class preambles:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-helvetica-tabularray.el][latex-templates/elegant-helvetica-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond-tabularray.el][latex-templates/elegant-garamond-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-tabularray.el][latex-templates/beautiful-racket-tabularray.el]]
  - Tightened tabularray inner spacing to ~rowsep=0pt~.
  - Added explicit tabularray template controls for caption/continuation rendering (~SetTblrTemplate~ + ~DefTblrTemplate~ overrides) to keep output visually close to existing non-tabularray classes.
- Validation:
  - Re-exported the Ralph Lauren proposal with ~elegant-garamond-tabularray~ and confirmed:
    - no blank agenda table page,
    - no footer spill from the 2-hour table,
    - both agenda tables render in full with expected row content.

* 2026-02-26: Disable rare Garamond ligatures by default (remove ct join)
- Updated Garamond fontspec defaults to disable rare ligatures while keeping common ligatures:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond.el][latex-templates/elegant-garamond.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket.el][latex-templates/beautiful-racket.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond-tabularray.el][latex-templates/elegant-garamond-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-tabularray.el][latex-templates/beautiful-racket-tabularray.el]]
- Change made:
  - ~Ligatures={Common,Rare}~ -> ~Ligatures={Common}~
- Result:
  - Disables discretionary/rare joins like ~ct~ in “Connecting”.
  - Keeps standard/common ligatures (e.g., ~fi~, ~fl~).
  - Leaves ~Contextuals=Alternate~ unchanged.

* 2026-02-26: Add opt-in tabularray class family for modern table rendering
- Added new opt-in LaTeX classes in [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/][latex-templates/]]:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-helvetica-tabularray.el][elegant-helvetica-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond-tabularray.el][elegant-garamond-tabularray.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket-tabularray.el][beautiful-racket-tabularray.el]]
- Added shared exporter helper:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/jay-latex-table-tabularray.el][jay-latex-table-tabularray.el]]
- Behavior of the new helper:
  - Registers only the new ~*-tabularray~ classes (existing production classes unchanged).
  - Converts Org-exported ~tabular/tabularx/longtable~ output into ~tblr/longtblr~.
  - Preserves existing opt-out semantics via =#+ATTR_LATEX: :wrap nil=.
- Class-level table defaults:
  - Loads ~tabularray~ and ~\\UseTblrLibrary{booktabs}~.
  - Sets baseline inner spacing/vertical centering with ~\\SetTblrInner{rows={valign=m}, rowsep=1.5pt, colsep=4pt}~.
- Validation performed:
  - Batch-loaded all new class files and helper in Emacs with ~ox-latex~.
  - Org export smoke tests confirmed conversion emits ~\\begin{tblr}~ for all three new classes.
  - XeLaTeX compile sanity check passed for exported sample (no hard LaTeX errors).

* 2026-02-26: LaTeX cross-template consistency pass (quotes, subtitles, tables, OT features)
- Updated the three primary proposal classes:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant.el][latex-templates/elegant.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond.el][latex-templates/elegant-garamond.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket.el][latex-templates/beautiful-racket.el]]
- Quote handling consistency:
  - Added ~csquotes~ + ~\\MakeOuterQuote{\"}~ to ~elegant~ and ~elegant-garamond~ so straight quotes render as smart opening/closing quotes (matching ~beautiful-racket~ behavior).
- Subtitle environment consistency:
  - Added ~subtitle~, ~subsubtitle~, and ~subsubsubtitle~ environments to ~elegant~ so all three templates expose the same subtitle helper pattern.
- Table feature consistency:
  - In ~beautiful-racket~, added ~longtable~ package support.
  - Added missing ~Z~ (centered) and ~W~ (right-aligned) tabularx column types in ~beautiful-racket~ to match ~elegant*~.
  - Normalized ~\\tabularxcolumn~ in ~beautiful-racket~ to ~m{#1}~ for consistent vertical alignment behavior with the other classes.
- OpenType defaults for Garamond classes:
  - Enabled Garamond feature defaults in ~elegant-garamond~ (~Numbers=OldStyle~, ~Ligatures={Common,Rare}~, ~Contextuals=Alternate~), matching existing ~beautiful-racket~ defaults.
- Validation:
  - Batch-loaded all three class files with ~ox-latex~ in Emacs; all loaded cleanly with no Lisp load errors.

* 2026-02-26: LaTeX table export hardening + shared wrapper refactor
- Added/updated table-export behavior across:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant.el][latex-templates/elegant.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/elegant-garamond.el][latex-templates/elegant-garamond.el]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/beautiful-racket.el][latex-templates/beautiful-racket.el]]
- Introduced shared helper [[file+emacs:/Users/jay/emacs/emacs-settings/latex-templates/jay-latex-table-wrap.el][latex-templates/jay-latex-table-wrap.el]] so table wrapping logic is no longer duplicated in each class file.
- Current default behavior for those classes:
  - Plain Org tables export as wrapping ~tabularx~ tables.
  - Wrapped tables use left-aligned columns with hyphenation-friendly ragged text.
  - Wrapped tables get light booktabs rules (~\\toprule~, ~\\midrule~, ~\\bottomrule~).
  - Per-table opt-out works via =#+ATTR_LATEX: :wrap nil= (keeps native ~tabular~).
- Preamble cleanup in the same pass:
  - Standardized proposal margins to 1 inch in elegant classes.
  - Removed deprecated ~tabu~ from ~elegant.el~.
  - Fixed duplicate package loads and malformed LaTeX snippets in ~beautiful-racket.el~.
  - Switched ~org-latex-to-pdf-process~ to ~org-latex-pdf-process~ where needed.
  - Added missing breakability/cleanup items in quote and preamble blocks (see trace doc below).
- Full detailed trace + rollback plan:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/latex-table-export-trace.org][docs/latex-table-export-trace.org]]
- Commit chain for this sequence (oldest → newest):
  - =476e5e10=, =2accd3d5=, =672fe56e=, =be740319=, =142662af=, =416b2648=, =cfd8a168=

* 2026-02-24: Unify sprint timer engine on prolific
- Replaced in-Emacs revenue timer logic in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-core.el][jay-org-roam-core.el]] with a process bridge to prolific:
  - =jay/prolific-pomo-start= launches [[file+emacs:/Users/jay/Dropbox/github/writing-trackers/prolific/bin/pomo][bin/pomo]]
  - default block size is now 3 sprints (roughly 90 minutes)
  - =s-u R= starts prolific block, =s-u C-r= stops it
- Updated legacy Pomodoro keypaths in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] to point at the same prolific commands:
  - =<f20>=, =s-k p s=, =C-c C-x p p=, =C-c C-x pi= → =jay/prolific-pomo-start=
  - =C-c C-x po= → =jay/prolific-pomo-stop=
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] so compatibility entrypoints now route to prolific:
  - =pomodoro-start= and =pomodoro-stop=
  - =pomidor-start= now delegates to =pomodoro-start=
- Updated docs for the new canonical flow:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/goals-system.org][docs/goals-system.org]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]]
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/time-and-date-functions.org][docs/time-and-date-functions.org]]
- Result: one canonical sprint engine (prolific), while keeping existing muscle-memory keys intact.

* 2026-02-24: Keep s-g search; split goals features cleanly
- Refactored [[file+emacs:/Users/jay/emacs/emacs-settings/jay-goals-system.el][jay-goals-system.el]] to *rep-counter only*:
  - Weekly reps tracking (outreach/followups/calls/posts)
  - Mode-line indicator: =[O:2/10 F:8/15 C:1/5 P:1/2]=
  - Persistence to [[file+emacs:/Users/jay/Dropbox/roam/velocity/tracking/weekly-reps.org][weekly-reps.org]]
  - Keybindings under =s-M-g=: =1/2/3/4/s/S/r=
- Preserved =s-g= for =isearch-repeat-forward= (muscle-memory safe).
- Confirmed workflow automation remains in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-core.el][jay-org-roam-core.el]]:
  - =s-u g= start day
  - =s-u A= anti-stall
  - =s-u R= revenue block start
  - =s-u C-r= revenue block stop
- Updated docs to reflect this split:
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/goals-system.org][docs/goals-system.org]]
  - goals hub and Claude instructions in roam notes.

* 2026-02-24: Add capture-cleanup ERT regression suite and smoke-test gate
- Added new ERT file [[file+emacs:/Users/jay/emacs/emacs-settings/tests/capture-cleanup-tests.el][tests/capture-cleanup-tests.el]] with focused regression coverage for =jay/org-web-tools-cleanup-string=:
  - Timestamp removal/preservation toggle
  - Social share link + mailto stripping
  - Relative-time and view-count removal
  - Bold-line to org-heading promotion
  - Heading/body spacing and whitespace-only line cleanup
- Updated smoke script [[file+emacs:/Users/jay/emacs/emacs-settings/scripts/emacs_smoke_test.sh][scripts/emacs_smoke_test.sh]] to run the capture cleanup ERT suite as part of the default batch flow.
- Smoke test now runs in 5 stages (with optional full-init stage retained via =--full-init=).
- Validation: ran =scripts/emacs_smoke_test.sh= and confirmed =6/6= capture cleanup ERT tests pass in the smoke pipeline.

* 2026-02-24: Add jay/doctor fix-hints mode
- Enhanced [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]] doctor workflow:
  - =M-x jay/doctor= now accepts prefix arg (=C-u=) to append a =Fix Hints= section
  - Added =M-x jay/doctor-fix-hints= convenience command
- Fix hints include actionable one-line remediations for:
  - Missing binaries (=brew install rg/pandoc/hunspell=)
  - Org-roam DB/path issues (=org-roam-db-sync= / rebuild guidance)
  - Dropbox CloudStorage path mismatch
  - Keybinding collision follow-up command
  - Recent startup/runtime error follow-up steps
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]] with usage examples.

* 2026-02-24: Add which-key descriptions for custom keybindings
- Added which-key labels in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] for main prefix groups:
  - =s-k= (commands), =s-/= (search), =s-u= (org-roam), =]= (extras)
  - Subgroups: =s-k o= (org), =s-k t= (tools), =s-k c= (clock), =s-k f= (frame), etc.
- Added org-roam-specific labels in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-core.el][jay-org-roam-core.el]]:
  - Profile shortcuts: =s-u 1/2/3/4= with profile names
  - Dailies: =s-u d= (today), =s-u y= (yesterday), =s-u .= (goto date)
  - Core commands: =s-u f= (find), =s-u i= (insert), =s-u c= (capture)
- Makes custom keybindings discoverable via which-key popup.

* 2026-02-24: Add jay/doctor diagnostics + startup timing dashboard
- Added =M-x jay/doctor= in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]].
  - Reports recent error-like startup messages
  - Checks required binaries (=rg=, =pandoc=, =hunspell=)
  - Reports org-roam DB status (directory, DB file existence/size, live connection)
  - Checks Dropbox path sanity (direct mount vs CloudStorage path)
  - Surfaces key-minor vs global keybinding collision count with samples
- Added startup timing instrumentation in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]]:
  - Wrapped key =load= / =org-babel-load-file= / =require= calls with timing helpers
  - Added startup hook that appends timing snapshots to [[file+emacs:/Users/jay/emacs/emacs-settings/docs/lab/startup-profile.org][docs/lab/startup-profile.org]]
  - Captures total init time plus per-module load times for regression tracking
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]] with =jay/doctor= usage and startup profile location.

* 2026-02-24: Add Embark context actions for org-roam and URLs
- Added Embark org-roam target actions in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-core.el][jay-org-roam-core.el]]:
  - =i= insert node link
  - =v= visit node
  - =r= refile to node
  - =b= toggle backlinks buffer
  - =a= add alias on target node
- Added URL Embark actions in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-core.el][jay-org-roam-core.el]]:
  - =y= copy URL to kill ring
  - =p= preview inline links/images in Org buffers
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]] with Embark action usage notes.

* 2026-02-24: Add repeat-mode for faster Org navigation
- Enabled =repeat-mode= globally in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]].
- Defined custom repeat maps for Org navigation (works on Org 9.6+):
  - Heading navigation: =C-c C-n/p=, then =n/p/f/b/u= to continue
  - Block navigation: =C-c M-f/b=, then =f/b= to continue
  - Link navigation: =C-c C-x C-n/p=, then =n/p= to continue
- When upgrading to Org 9.8, these become redundant (Org 9.8 has built-in repeat maps).

* 2026-02-24: Add startup health command and repeatable smoke script
- Removed always-on org-roam debug in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-core.el][jay-org-roam-core.el]]:
  - Replaced global =init-file-debug= toggle with =jay/org-roam-debug= (default =nil=).
  - Keeps optional diagnostics available without forcing noisy startup.
- Added =M-x jay/startup-health-check= in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]].
  - Reports Org version, =org-link-preview= availability, =org-support-shift-select= status, and org-roam fix-file found/loaded status.
- Added executable smoke-test script [[file+emacs:/Users/jay/emacs/emacs-settings/scripts/emacs_smoke_test.sh][scripts/emacs_smoke_test.sh]]:
  - Tangles core literate files
  - Runs =check-parens= on key files
  - Performs minimal runtime checks
  - Optional =--full-init= mode for a full startup pass
- Documented usage in [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][docs/instructions.org]] under Safe Testing.

* 2026-02-24: Archive docs/lab/ experimental org-roam code
- Moved experimental org-roam files to =archive/docs-lab/= (gitignored):
  - =org-roam-database-switching.org=
  - =org-roam-enhancements.org=
  - =org-roam-second-database.org=
- Added [[file+emacs:/Users/jay/emacs/emacs-settings/docs/lab/README.org][docs/lab/README.org]] pointer to current implementation in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/][jay-org-roam-suite/]].
- These files contained early prototypes that informed the suite design; archived for historical reference.

* 2026-02-24: Consolidate clipboard primitives into pasteboard module
- Moved low-level clipboard functions from [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] into [[file+emacs:/Users/jay/emacs/emacs-settings/pasteboard-copy-and-paste-functions.el][pasteboard-copy-and-paste-functions.el]]:
  - =clipboard-to-kill-ring=
  - =clipboard-to-kill-ring-and-strip-trailing-newlines=
  - =copy-region-as-kill-and-push-to-clipboard=
  - =yas/pasteboard-raw=
  - =pasteboard-copy-large-file=
- Left notes in shared-functions.org pointing to the pasteboard module.
- Kept higher-level Org link insertion helpers in shared-functions.org.

* 2026-02-24: Fix org-roam optional fix-file lookup paths
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-core.el][jay-org-roam-core.el]] to search both suite directory and parent =emacs-settings/= for optional fix files.
- Added =jay/org-roam--load-optional-fix= helper with clear logging.
- Fixes silent load failure for =org-roam-id-fix.el= and =org-roam-db-fix.el=.

* 2026-02-24: Deprecate org-roam-config.el into compatibility shim
- Replaced [[file+emacs:/Users/jay/emacs/emacs-settings/org-roam-config.el][org-roam-config.el]] implementation with a minimal compatibility shim that:
  - Adds =jay-org-roam-suite/= to load-path (when needed)
  - Shows a one-time deprecation message
  - Loads =jay-org-roam-core= (and =jay-editor-extras= if present)
  - Provides feature =org-roam-config= for backward compatibility
- Archived full legacy implementation to [[file+emacs:/Users/jay/emacs/emacs-settings/archive/org-roam-config.el_legacy_2026-02-24][archive/org-roam-config.el_legacy_2026-02-24]].
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/README.org][README.org]] to mark =org-roam-config.el= as a shim and link to the archive copy.

* 2026-02-24: Org obsolete API migration + 9.8-compatible preview fallback
- Updated Org/Emacs obsolete symbols in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]]:
  - =org-export-before-processing-hook= → =org-export-before-processing-functions=
  - =org-export-before-parsing-hook= → =org-export-before-parsing-functions=
  - =org-speed-commands-user= → =org-speed-commands=
  - =org-time-clocksum-format= → =org-duration-format=
  - =find-file-hooks= → =find-file-hook=
  - =org-match-string-no-properties= → =match-string-no-properties=
  - =org-add-link-type= → =org-link-set-parameters=
  - =org-make-link-string= → =org-link-make-string=
  - =org-preview-latex-fragment= → =org-toggle-latex-fragment=
- Added Org-version-safe inline image preview helper in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]]:
  - New =jay/org-preview-inline-images= uses =org-link-preview= on Org 9.8+, falls back to =org-display-inline-images= / =org-toggle-inline-images= on older Org.
  - Updated =org-show-inline-images= and clipboard image insertion to use the helper.
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/org-yt.el][org-yt.el]] for dual compatibility:
  - Keeps new =:preview= path for Org 9.8+
  - Adds fallback advice for =org-display-inline-images= when =org-link-preview= is unavailable (current runtime Org 9.6.x).
  - Unified link registration through =org-yt-url-protocol=.
- Updated hook link registration in [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.org][gnu-emacs-startup.org]]: =org-add-link-type= → =org-link-set-parameters=.
- Updated pasteboard obsoletes in [[file+emacs:/Users/jay/emacs/emacs-settings/pasteboard-copy-and-paste-functions.el][pasteboard-copy-and-paste-functions.el]]:
  - =x-select-text= → =gui-select-text=
  - =x-select-enable-clipboard= → =select-enable-clipboard=
- Validation:
  - Retangled [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] and [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.org][gnu-emacs-startup.org]]
  - Ran =check-parens= on edited/generated files
  - Obsolete-symbol scan is clean for active config files; remaining hits are in archive/vendor docs.

* 2026-02-11: Auto-advance to next checkbox after toggle
- Added advice to =org-ctrl-c-ctrl-c= and =org-toggle-checkbox= in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] so that toggling a checkbox with =C-c C-c= automatically moves to the next list item.
- Uses =my/with-advice= macro for temporary advice scoping around =org-update-checkbox-count-maybe=.
- Cursor lands at the beginning of the next item's text.

* 2026-02-10: Remove redundant asterisks from pasted headings
- Added =pasteboard--remove-redundant-heading-asterisks= in [[file+emacs:/Users/jay/emacs/emacs-settings/pasteboard-copy-and-paste-functions.el][pasteboard-copy-and-paste-functions.el]] to strip trailing whitespace-separated asterisks (e.g., =** Heading **= → =** Heading=) while preserving inline emphasis.
- Added whitespace-guarded heading cleanup to [[file+emacs:/Users/jay/emacs/emacs-settings/elpa-supplement/chatgpt2org.el][chatgpt2org.el]] and [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]].
- Runs automatically during =pasteboard-paste-adaptive=, =chatgpt2org= / =html2org-clipboard=, and =jay/org-web-tools-*-clean=.

* 2026-02-10: Guard trailing heading asterisk cleanup
- Tightened the trailing-asterisk regex to require whitespace before the decorative stars, preserving inline emphasis and embedded asterisks (e.g., =*emphasis*=, =A* Search=).

* 2026-02-10: Enhanced web content cleanup with heading spacing
- Added cleanup rules across all paste/capture methods:
  - Strip relative timestamps like "2 years ago", "3 months ago"
  - Remove lines containing only whitespace
  - Remove blank lines between headings and body text (preserve between headings)
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]]:
  - Enhanced =jay/org-web-tools-cleanup-string= with new rules
  - Archived old =html2org-clipboard= EXAMPLE blocks (now aliased to chatgpt2org)
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/elpa-supplement/chatgpt2org.el][chatgpt2org.el]] with same cleanup rules
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/pasteboard-copy-and-paste-functions.el][pasteboard-copy-and-paste-functions.el]]:
  - Added =pasteboard--remove-blank-line-after-headings= function
  - Called in =pasteboard--clean-string= after bold-to-heading conversion
- Org-mode style: headings flow directly into body text without blank line separation

* 2026-02-10: Install org-web-tools and add social share link cleanup
- Installed [[file+emacs:/Users/jay/emacs/emacs-settings/org-web-tools-master/][org-web-tools]] for capturing web pages as org-mode content
- Added dependencies to =dotspacemacs-additional-packages=: =compat=, =esxml=, =plz=, =request=
- Added load-path and require in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][init.el]]
- Enhanced [[file+emacs:/Users/jay/emacs/emacs-settings/elpa-supplement/chatgpt2org.el][chatgpt2org.el]] with social share link cleanup:
  - Remove =[[url][//]]= patterns (icon-only link descriptions)
  - Remove nested base64 images in link descriptions
  - Remove common social share domains (facebook, twitter, linkedin, pinterest, whatsapp, xing)
  - Remove mailto share links
  - Remove view counts like =//6,728=
- Added org-web-tools cleanup functions to [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]]:
  - =jay/get-url-from-chrome= - get URL from Chrome's frontmost tab via AppleScript
  - =jay/get-url-from-clipboard-or-chrome= - get URL from Chrome, clipboard, kill-ring, or prompt
  - =jay/org-web-tools-cleanup-string= - clean cruft from org content
  - =jay/org-web-tools-cleanup-buffer= - clean current buffer
  - =jay/org-web-tools-read-url-as-org-clean= - fetch URL with cleanup
  - =jay/org-web-tools-insert-web-page-as-entry-clean= - insert as heading with cleanup
- Usage: Just have the page open in Chrome and run =jay/org-web-tools-read-url-as-org-clean=
- Customization: =jay/org-web-tools-include-timestamp= (default =nil=) - set to =t= to keep capture timestamps
- Bold-to-heading conversion: all three paste methods now convert bold lines that look like headings to actual org headings:
  - =*1. Title:*= becomes =*** 1. Title:=
  - =*Heading*= becomes =*** Heading=
  - Added to: =jay/org-web-tools-cleanup-string=, =chatgpt2org=, and =pasteboard--convert-bold-to-headings=
- Key difference between tools:
  - =html2org-clipboard= / =chatgpt2org=: pastes clipboard HTML (what you copied)
  - =org-web-tools=: fetches URL and uses =eww-readable= to extract main article content

* 2026-02-10: Fix keybinding frequency report and yasnippet conflict

- Fixed =my/keybinding-frequency-report= in [[file+emacs:/Users/jay/emacs/emacs-settings/keybinding-docs.el][keybinding-docs.el]]:
  - Changed =(keyfreq-table)= to =keyfreq-table= (it's a variable, not a function)
  - Added =(keyfreq-table-load keyfreq-table)= to load saved data from disk before generating report
  - Enhanced to resolve bindings from active keymaps per recorded major mode (global + local + minor)
  - Added =my/--load-keyfreq-table= to read saved keyfreq file even when package not loaded
  - Added =my/prime-key-unavailable= alist for keys reserved by macOS (e.g., s-d for Command-Tab)
- Fixed =C-c e= keybinding conflict:
  - =yasnippet= use-package block had global binding =("C-c e" . yas-load-snippet-buffer)= overriding mode-specific =eval-buffer= / =eval-adaptive= bindings
  - Changed to =:map snippet-mode-map= so it only applies in snippet-mode
  - Edit in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] line 2532
- Generated first [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-frequency-report.org][keybinding-frequency-report.org]] from accumulated keyfreq data
- Created [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-analysis.org][keybinding-analysis.org]] with:
  - Top 35 most-used commands with usage counts
  - Underused prime keys (8 with zero usage, 10 with 1-10 uses)
  - Recommendations for reassigning wasted s-KEY bindings
  - Updated with corrections: s-d reserved by macOS, s-j kept as primary org-todo key
- Follow-up refinements:
  - Frequency report now resolves bindings via active keymaps per recorded mode (plus minibuffer maps), not just custom alists
  - Ergonomic mismatch list now flags only when the *best* binding is triple/quad-key
  - Added reserved-key exclusion list and documented s-d as unavailable (Command-Tab)
  - Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-consolidation-plan.org][keybinding-consolidation-plan.org]] with the macOS reserved-key constraint
  - Rebound ~s-P~ to =jay/org-web-tools-insert-web-page-as-entry-clean= in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]]
  - Rebound ~M-o~ to =org-mode= in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] (replaces =embolden-or-bold=)
  - Updated keybinding docs/report and added the binding to the memorize list:
    - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-frequency-report.org][keybinding-frequency-report.org]]
    - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-analysis.org][keybinding-analysis.org]]
    - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/macOS-command-key-bindings.org][macOS-command-key-bindings.org]]
    - [[file+emacs:/Users/jay/Dropbox/roam/notes/20230408212913-emacs_keybindings_to_remember_to_use.org][20230408212913-emacs_keybindings_to_remember_to_use.org]]
    - Regenerated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-dump.org][keybinding-dump.org]] and [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-reference.org][keybinding-reference.org]]

* 2026-02-03: Sort org-roam capture templates alphabetically
- Sorted all capture template lists in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-org-roam-suite/jay-org-roam-profiles.el][jay-org-roam-profiles.el]] alphabetically by key (ASCII order: =$= first, then uppercase A-Z, then lowercase a-z)
- Affected lists:
  - =jay/org-roam-default-template-specs= (20 templates)
  - =jay/org-roam-default-factory-template-specs= (19 templates)
  - =jay/org-roam-capture-templates-mylife= (13 templates)
  - =jay/org-roam-capture-templates-social= (10 templates)
  - =jay/org-roam-capture-templates-parents= (6 templates)

* 2026-01-29: Fix keys.el not being loaded
- All bindings in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] were not working (e.g., ~s-v~ → =pasteboard-paste-adaptive=)
- Root cause: during the keybinding consolidation, bindings were migrated to =keys.el= but no one added a =(load ...)= statement to actually load the file
- Fix: added =(load "/Users/jay/emacs/emacs-settings/keys.el")= to [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]] after =jay-osx.el= is loaded

* 2026-01-28: Fix 12 incorrectly resolved keybinding conflicts from migration
- The keybinding consolidation into [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] had incorrectly resolved conflicts: when the same key was bound in both keys.el and a .org file (gnu-emacs-startup.org or shared-functions.org), the .org file's binding should have won (since it loaded after keys.el), but the migration kept the keys.el binding instead.
- Fixed 12 bindings in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]]:
  - ~C-x C-j~ :: =consult-dir= → =jay-up-directory= (gnu-emacs-startup.org)
  - ~s-k g l~ :: =affe-grep-gnulisp-directory= → =goto-line= (gnu-emacs-startup.org)
  - ~s-k y a~ :: =affe-grep-gnulisp-directory= → =new-yasnippet= (shared-functions.org)
  - ~s-V~ :: =pasteboard-paste-adjusted-subtrees-adaptive= → =pasteboard-paste-verbatim= (gnu-emacs-startup.org)
  - ~s-T~ :: =mw-thesaurus-lookup-dwim= → =new-buffer-in-new-pane= (gnu-emacs-startup.org)
  - ~M-[~ :: =org-previous-visible-heading= → =org-backward-heading-same-level= (shared-functions.org)
  - ~M-]~ :: =org-next-visible-heading= → =org-forward-heading-same-level= (shared-functions.org)
  - ~s-k r l~ :: =remove-link= (nonexistent) → =remove-hyperlink-at-point= (gnu-emacs-startup.org)
  - ~<s-return>~ :: =toggle-fullscreen= (nonexistent) → =jay/toggle-fullscreen= (gnu-emacs-startup.org)
  - ~C-c C-x po~ :: =pomidor-stop= → =pomodoro-stop= (gnu-emacs-startup.org)
  - ~M-t~ :: restored to =titlecase-dwim=; moved =mw-thesaurus-lookup-dwim= to new binding ~s-k t h~
  - ~C-x C-d~ :: =dired= → =consult-dir= (Jay's preference)
- Removed 3 fabricated bindings that never existed pre-migration:
  - ~s-{~ → =path-copy-path-to-clipboard= (removed)
  - ~s-}~ → =path-copy-path-to-clipboard= (restored to =poetry-rhyme-word=)
  - ~C-x m~ → =endless/mc-map= (removed; was only on ~C-8~)
- Deleted redundant [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybindings-master.org][keybindings-master.org]] — the dump/annotations/reference pipeline already covers it
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-reference.org][keybinding-reference.org]] as the canonical human-readable keybinding doc
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-dump.org][keybinding-dump.org]] with corrected bindings

* 2026-01-27: Fix =[= key blocked by bad kbd syntax
- =[= was acting as a prefix key instead of self-inserting
- Root cause: =("[mouse-2]" . context-menu-open)= in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] — =kbd= parsed ="[mouse-2]"= as literal keystrokes =[=, =m=, =o=, =u=, =s=, =e=, =-=, =2=, =]= making =[= a prefix
- Fix: changed to ="<mouse-2>"= which is the correct =kbd= syntax for mouse events

* 2026-01-27: Fix spacecraft-mode.org broken src block
- =endless/upcase=, =endless/downcase=, =endless/capitalize=, =capitalize-or-endless/capitalize= were missing from [[file+emacs:/Users/jay/emacs/emacs-settings/spacecraft-mode.el][spacecraft-mode.el]], causing autoload errors
- Root cause: a top-level =* heading= at line 310 of [[file+emacs:/Users/jay/emacs/emacs-settings/spacecraft-mode.org][spacecraft-mode.org]] was inside a =#+begin_src= block (opened line 238, closed line 312), breaking org's block parsing so these functions never tangled
- Fix: moved =#+end_src= to before the heading; retangled via =org-babel-load-file= and verified all functions present in the =.el=

* 2026-01-27: Fix s-k prefix clobbered by gnu-emacs-startup.el
- All =s-k= sequences (=s-k s-k=, =s-k g l=, etc.) were broken after restart
- Root cause: [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.el][gnu-emacs-startup.el]] lines 150-153 ran =(fset 's-k-prefix s-k-map)= inside =(with-eval-after-load 'evil ...)=, creating a fresh empty keymap that overwrote the populated prefix map keys.el had already installed
- Fix: removed the =s-k-map= / =fset= block from both [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.el][gnu-emacs-startup.el]] and [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.org][gnu-emacs-startup.org]]; keys.el now solely owns the =s-k-prefix= definition
- Also in this session:
  - Fixed =key-minor-mode= permanently disabled after first minibuffer use (added =minibuffer-exit-hook= to re-enable)
  - Restored =<s-left>= / =<s-right>= → =buffer-stack-up= / =buffer-stack-down= (dropped during earlier migration)
  - Uncommented =ivy-smex.el= load in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]]
  - Confirmed =org-roam-config.el= is dead code (not loaded); =jay-org-roam-core.el= is authoritative
  - Audit found only ~52 bindings remaining to migrate (26 in jay-org-roam-core.el, 25 stale in org-roam-config.el, 1 in shared-functions.org)

* 2026-01-27: Keybinding documentation system & keys.el cleanup
- Resolved 6 issues in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]]:
  1. Removed duplicate help bindings
  2. Fixed ~s->~ conflict (kept =load-gnu-startup=, removed =org-mark-subtree=)
  3. Fixed ~C-s-k~ conflict (kept =org-cut-subtree=)
  4. Fixed ~C-S-return~ duplicate in org mode-local block
  5. Replaced all lambda bindings with named wrapper functions (=my/org-show-level-N=, =my/org-agenda-day=, =my/panic-save-and-quit=, =my/scroll-up=, =my/scroll-down=)
  6. Redistributed "Migrated Bindings" grab-bag into thematic sections and deleted the catch-all section
- Refactored Section 5 (mode-local bindings) from procedural ~define-key~ calls into declarative alists with ~my/mode-binding-registry~ for introspection
- Created [[file+emacs:/Users/jay/emacs/emacs-settings/keybinding-docs.el][keybinding-docs.el]] with:
  - =keyfreq= integration for silent usage tracking
  - =M-x my/keybinding-dump= — auto-generates [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-dump.org][keybinding-dump.org]] from declarative alists
  - =M-x my/keybinding-merge= — merges dump with [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-annotations.org][keybinding-annotations.org]] to produce [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-reference.org][keybinding-reference.org]], flags =[NEW]= and =[REMOVED]= bindings
  - =M-x my/keybinding-frequency-report= — generates [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-frequency-report.org][keybinding-frequency-report.org]] with most-used commands, ergonomic mismatches, and underused prime keys
- Added =keyfreq= to =dotspacemacs-additional-packages= in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][init.el]]
- Added load call for [[file+emacs:/Users/jay/emacs/emacs-settings/keybinding-docs.el][keybinding-docs.el]] in [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs.d/init.el][init.el]] user-config
- Created [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-annotations.org][keybinding-annotations.org]] skeleton with initial annotations
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/macOS-command-key-bindings.org][macOS-command-key-bindings.org]]: converted tables to description lists, fixed stale entries (=s->= now =load-gnu-startup=, =s-k a d= now =my/org-agenda-day=), added cross-reference to keybinding-reference.org

* 2026-01-27: Keybinding migration regression (fixed) — apology
- I broke several keybindings during the migration. Sorry.
- Root causes:
  - I redefined =key-minor-mode= in [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]], overwriting the map defined in [[file+emacs:/Users/jay/emacs/emacs-settings/jay-osx.el][jay-osx.el]].
  - I installed ~s-k~ sub-keys before ensuring ~s-k~ was a prefix, which raised "starts with non-prefix key" and aborted all key installs.
  - I changed ~=~ to =smex= (non-command here); it should have remained =amx=.
- Fixes applied:
  - Removed the duplicate =key-minor-mode= definition from =keys.el=.
  - Ensured ~s-k~ is a prefix in =key-minor-mode-map= before installing bindings.
  - Restored ~=~ → =amx=; verified ~s-<left>/<right>~ → =buffer-stack-up/down= and other core bindings.

* 2026-01-27: Audit of fabricated bindings during consolidation
- Verified pre-migration baseline and compared against current =keys.el=.
- Found one fabricated binding added during consolidation: ~org-mode~-local =-= → =smart-hyphen= (this did not exist pre-migration).
- Removed the fabricated binding to restore pre-migration behavior.

* 2026-01-27: Plan keybinding consolidation into keys.el
- Created [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybinding-consolidation-plan.org][keybinding-consolidation-plan.org]] with full migration plan
- Goal: move all ~define-key~, ~global-set-key~, ~define-super-key~, and ~spacemacs/set-leader-keys~ calls into [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]] as the single canonical keybinding location
- Scope: ~670+ bindings across 7 source files (gnu-emacs-startup.org, shared-functions.org, search-commands.org, spacemacs-new-config.el, org-roam-config.el, spacecraft-mode.org, jay-org-roam-core.el)
- [[file+emacs:/Users/jay/emacs/emacs-settings/jay-osx.el][jay-osx.el]] preserved as-is
- Identified ~C-c d~ conflict: ~crux-duplicate-current-line-or-region~ vs ~insert-todays-date~
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/instructions.org][instructions.org]]: added org-mode links to all file references, updated Changing Keybindings section
- In-progress migrations already completed:
  - Moved bindings from [[file+emacs:/Users/jay/emacs/emacs-settings/search-commands.org][search-commands.org]], [[file+emacs:/Users/jay/emacs/emacs-settings/spacemacs-new-config.el][spacemacs-new-config.el]], [[file+emacs:/Users/jay/emacs/emacs-settings/goals-agenda.el][goals-agenda.el]], and [[file+emacs:/Users/jay/emacs/emacs-settings/spacecraft-mode.org][spacecraft-mode.org]] into [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]]
  - Smart punctuation org-mode bindings (~smart-space~, ~smart-period~, ~comma-or-smart-comma~, ~smart-question-mark~, ~smart-exclamation-point~, ~smart-hyphen~, ~smart-semicolon~, ~colon-or-smart-colon~) moved into ~with-eval-after-load 'org~ block in keys.el
  - Case-change bindings (~M-C~ → ~capitalize-word~, ~M-L~ → ~downcase-sentence~) moved into ~my/global-key-bindings~ alist
- Continued migration into [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]]:
  - Moved remaining shared-functions bindings (org backtick correction, palimpsest ~C-c C-r~, org-src ~C-c C-c~, Info/winner/ctrlf/markdown maps, minibuffer ~C-g~, multiple-cursors prefix map, panic button ~C-c C-!~, and ~C-x m~)
  - Moved ~C-c <mouse-3>~ right-click context menu binding into global map bindings
- Updated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/macOS-command-key-bindings.org][macOS-command-key-bindings.org]] by regenerating s- / s-/ / s-k tables from [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]]
- Generated [[file+emacs:/Users/jay/emacs/emacs-settings/docs/keybindings-master.org][keybindings-master.org]] as a full custom keybinding catalog from [[file+emacs:/Users/jay/emacs/emacs-settings/keys.el][keys.el]]
- Cleaned broken binding stubs in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] and [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.org][gnu-emacs-startup.org]] created by earlier removals
- Retangled [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] and [[file+emacs:/Users/jay/emacs/emacs-settings/gnu-emacs-startup.org][gnu-emacs-startup.org]] and reran ~check-parens~ on both generated .el files

* 2026-01-15: Corrective note on commit instruction misunderstanding
- Added a corrective note acknowledging a mistaken response about committing all changes when asked

* 2026-01-15: Move search/completion stack into search-commands.org
- Moved isearch, ivy/swiper, consult, orderless/vertico/embark, counsel, and related helpers out of =shared-functions.org=
- Consolidated search utilities (rg/deadgrep, consult-locate, counsel-fd, consult ripgrep advice, org heading search) into =search-commands.org=
- Removed redundant mini vertico stanza from projectile section after the move

* 2026-01-15: Document optional shared-functions.org split candidates
- Noted low-coupling extraction options in =docs/design-architecture.org=:
  keybindings/key-chord, search/completion, org-roam stubs, UI/appearance, and web-research helpers

* 2025-12-31: Make org-emphasis-regexp-components survive resets
- Previous fix (2025-12-26) only applied settings once at org load time
- Problem: Spacemacs functions like ~spacemacs/prettify-org-buffer~ and ~space-doc-mode~ were resetting the emphasis regex
- Solution in ~spacemacs-new-config.el~:
  1. Store settings in ~jay/org-emphasis-regexp-components~ defvar
  2. Create ~jay/apply-org-emphasis-settings~ function to reapply
  3. Hook into ~org-mode-hook~ to reapply on every org buffer
  4. Add ~:after~ advice on ~spacemacs/prettify-org-buffer~ and ~spacemacs//space-doc-alternative-emphasis~
- Now emphasis settings survive org reloads, new buffers, and Spacemacs overrides

* 2025-12-31: Update recovery-guide.org for GUI Emacs
- All commands now use explicit ~/Applications/Emacs.app/Contents/MacOS/Emacs~ path
- Added recovery aliases to ~jay-scripts.sh~: ~emacs-debug~, ~emacs-vanilla~, ~emacs-check~
- Restructured guide with clear "When to Use" intro and "Try this if" context for each step
- Quick reference table now shows when to use each step, not just what to do
- Moved technical details to appendices (batch syntax check, error patterns, prevention)

* 2025-12-31: Sync recovery docs and source-of-truth references
- Updated Spacemacs path references to =/Users/jay/emacs/emacs-settings/spacemacs.d/init.el= and =~/.spacemacs.d= symlink guidance
- Switched pasteboard documentation to the pure elisp source (=pasteboard-copy-and-paste-functions.el=) and removed stale .org references
- Fixed diary file path to =/Users/jay/Dropbox/github/timeless/data/timeline.md= in calendar docs
- Resolved duplicate =s->= binding in =docs/macOS-command-key-bindings.org= (kept =load-gnu-startup=)
- Added =docs/recovery-recommendations-codex.org= with recovery audit and recommended improvements

* 2025-12-31: Implement known-good fallback (manual)
- Tagged current config as =known-good= and created worktree at =~/emacs/emacs-settings-known-good=
- Aligned =docs/recovery-guide.org= to use the long-form recovery aliases (=emacs-debug=, =emacs-vanilla=, =emacs-min=, =emacs-known-good=)

* 2025-12-31: Create recovery guide and minimal init
- Created ~docs/recovery-guide.org~ with 5-level escalation path for when Emacs won't start
- Created ~minimal-init.el~ escape hatch for bypassing Spacemacs entirely. It now lives at [[file+emacs:/Users/jay/emacs/emacs-settings/archive/minimal-init.el][archive/minimal-init.el]].
  - Usage: ~emacs -Q -l ~/emacs/emacs-settings/archive/minimal-init.el~
  - Provides basic org-mode, macOS keybindings, dark theme
  - No packages, no complexity - emergency editing only

* 2025-12-31: Backup system audit
- Audited backup system and found critical issues:
  - 1,906 unpushed commits to GitHub (fixed with this push)
  - Documentation claimed Dropbox sync but emacs-settings not in Dropbox
  - ~backup-system.org~ said ~kept-new-versions 50~ but actual config has ~9~
  - Recovery scripts mentioned in docs don't exist
  - ~spacemacs.after-incident-20251005~ still exists (should be deleted)
- Verified working backup layers:
  - Per-save backups: 9,132 files, 185MB in ~~/emacs/backup/per-save/~
  - Per-session backups: 2,895 files, 76MB in ~~/.emacs.d/backup/per-session/~
  - Time Machine configured to "Remnant"

* 2025-12-31: Major documentation consolidation
- Reduced docs/ from 14+ files to 11 by eliminating redundancy
- Merged files:
  - ~suggested-next-steps.org~ into ~critical-next-steps.org~ (combined roadmap)
  - ~the-emacs-settings-approach.org~ into ~design-architecture.org~ (added philosophy section)
  - ~visual-design-philosophy.org~ into ~instructions.org~ (added visual design section)
  - ~pasteboard-markdown-detection.org~ into ~pasteboard-architecture.org~
- Deleted redundant files:
  - ~ORG-ROAM-PROFILES-QUICKSTART.org~ (README.md sufficient)
  - ~CHANGE-LOG.org~ (redundant with work-log.org)
- Extracted key lessons and deleted:
  - ~spacemacs-incident-20251005.org~ → symlink safety lesson added to ~codebase-wisdom.org~
  - [[file+emacs:/Users/jay/emacs/emacs-settings/docs/info-system-debugging.org][docs/info-system-debugging.org]] → info build-from-source lesson added to ~codebase-wisdom.org~
- Remaining core docs: instructions, design-architecture, codebase-wisdom, critical-next-steps, backup-system, pasteboard-architecture, work-log

* 2025-12-31: Refactor shared-functions.org and archive unused code
- Refactored `org-show-level-X` functions in `shared-functions.org`:
  - Replaced repetitive definitions for levels 1-8 with a single `org-show-level` function taking an argument.
  - Updated keybindings to use lambda functions calling `org-show-level`.
- Archived unused code:
  - Moved unused `my-org-export-change-options` examples to [[file+emacs:/Users/jay/emacs/emacs-settings/archive/shared-functions-archive.org][archive/shared-functions-archive.org]].
- Verified `chatgpt2org` alias points to existing `elpa-supplement/chatgpt2org.el`.
- Replaced hardcoded `~/Dropbox` paths with `jay/dropbox-dir` variable for portability.
- Optimized `buffer-stack-filter-regexp` using `regexp-opt` for better readability and performance.

* 2025-12-26: Fix org-emphasis-regexp-components not taking effect
- Fixed org emphasis (bold, italic) not being fontified in some buffers
- Root cause: Setting ~org-emphasis-regexp-components~ alone doesn't rebuild the cached regex
- Added explicit call to ~org-set-emph-re~ after org loads in ~spacemacs-new-config.el~:
  #+begin_src elisp
  (setq org-emphasis-regexp-components
        '("-[:space:]('\"{" "-[:space:].,:!?;'\")}\\[" "[:space:]" "." 1))
  (with-eval-after-load 'org
    (org-set-emph-re 'org-emphasis-regexp-components org-emphasis-regexp-components))
  #+end_src
- Key insight: Org caches the compiled emphasis regex; you must call ~org-set-emph-re~ to rebuild it after changing components

* 2025-12-20: Fix pasteboard-paste-clean stripping double line breaks
- Fixed ~pasteboard--strip-trailing-whitespace~ in ~pasteboard-copy-and-paste-functions.el~
- Changed regex from ~[[:space:]]+$~ to ~[ \t]+$~
- The ~[[:space:]]~ character class in Emacs includes newlines, causing blank lines to be stripped
- The fix uses ~[ \t]~ to match only horizontal whitespace (spaces and tabs), preserving paragraph breaks

* 2025-12-17: Rename functions for clarity and add new-buffer-in-new-pane
- Added ~new-buffer-in-new-pane~ function: splits window left and creates new org buffer
- Updated ~clone-indirect-buffer-new-window-and-focus~ to narrow both panes to subtree
- Renamed ~remove-links/jay~ → ~remove-hyperlink-at-point~
- Renamed 8 functions to use descriptive names instead of author prefixes:
  - ~dcaps-to-scaps~ → ~fix-double-capitals~
  - ~ap/org-count-words~ → ~org-count-words-in-subtree~
  - ~ndk/checkbox-list-complete~ → ~org-checkbox-list-complete~
  - ~endless/replace-apostrophe~ → ~replace-curly-apostrophe~
  - ~endless/replace-quote~ → ~replace-curly-quotes~
  - ~xah-convert-entities-to-html-chars-region~ → ~html-entities-to-chars~
  - ~xah-convert-html-chars-to-entities-region~ → ~html-chars-to-entities~
  - ~er-google~ → ~google-search-region~
- Updated keybindings:
  - ~M-t~ now bound to ~mw-thesaurus-lookup-dwim~
  - ~s-T~ now bound to ~new-buffer-in-new-pane~

* 2025-12-15: Guard native-comp settings for non-native-comp Emacs builds
- Fixed "Symbol's value as variable is void: native-comp-eln-load-path" error on startup
- Wrapped all native-comp settings in ~spacemacs.d/init.el~ with ~(when (boundp 'native-comp-eln-load-path) ...)~
- Settings guarded: ~native-comp-eln-load-path~, ~package-native-compile~, ~native-comp-jit-compilation~, ~native-comp-deferred-compilation~, ~native-comp-async-report-warnings-errors~
- Moved GC tuning (~gc-cons-threshold~, ~gc-cons-percentage~) outside the guard since those apply to all builds
- Current macOS Emacs cask build lacks native-comp support; these settings will auto-activate when switching to a native-comp build in the future

* 2025-12-11: Fix flyspell infinite recursion bug
- Fixed critical infinite recursion bug in ~flyspell-lazy-check-pending~ function in ~shared-functions.org~
  - Bug occurred when processing large files (200MB+) in fundamental mode
  - Added conditional return using ~(cl-return-from flyspell-lazy-check-pending nil)~
  - Only applies fix when idle timer is nil to avoid disrupting normal operations
  - Prevents Emacs from hanging with "Lisp nesting exceeds max-lisp-eval-depth" errors
- Updated keybindings in ~gnu-emacs-startup.org~:
  - Reverted ~s-K~ back to ~org-cut-subtree~ (was ~kill-region~)
  - Reverted ~s-M~ back to ~tr-toggle-transclusion~ (was ~capitalize-region-or-word~)
  - These changes better support org-transclusion workflow

* 2025-12-09: Org-transclusion documentation and social profile setup
- Created ~/docs/org-transclusion-guide.org~ with comprehensive documentation:
  - Quick reference for keybindings (~s-K~, ~S-s-<down>~, ~s-u t~)
  - Step-by-step walkthrough for transcluding headings between files
  - Commands reference for power-pack functions (~tr-toggle-transclusion~, etc.)
  - Troubleshooting section
- Updated ~roam-social~ profile in ~jay-org-roam-profiles.el~:
  - Changed directory from ~/Users/jay/Dropbox/roam-social~ to ~/Users/jay/Dropbox/social~
  - Points to existing social principles files instead of empty roam-social directory
- Added org-roam frontmatter (ID + title + filetags) to 8 files in ~/Users/jay/Dropbox/social~:
  - ~social-principles-streamlined.org~, ~conan-examples.org~, ~find-the-game.org~
  - ~gemini-how-to-improv.org~, ~advanced-principles.org~, ~in-person-principles.org~
  - ~index.org~, ~nyc-locations-master.org~
- Created ~/Users/jay/Dropbox/social/journal/~ directory for dailies
- Removed blue background shading from transcluded content in ~shared-functions.org~

* 2025-12-07: Update quote environment in elegant-garamond LaTeX template
- Styled quote environment to match ~beautiful-racket~ template:
  - Powder blue background (#f5f7ff), stormy blue-grey frame (#708090), moon rock grey text (#5D5D5D)
  - Centered block with left-aligned text
  - Breakable across pages
- Added paragraph spacing inside quotes using ~before upper={\setlength{\parskip}{1em}}~
- Set font to ~\footnotesize~ with ~\raggedright~ alignment
- Fixed deprecated ~org-latex-to-pdf-process~ → ~org-latex-pdf-process~ in ~beautiful-racket.el~
- Renamed original to ~beautiful-racket-classic.el~, promoted Tufte version to ~beautiful-racket.el~
- ~beautiful-racket.el~ now includes Tufte-style typography enhancements:
  - Letterspacing for small caps via ~letterspace~ package (~\textsc~, ~\smallcaps~, ~\allcaps~)
  - ~ragged2e~ package for better ragged right with hyphenation
  - ~textcase~ package for ~\MakeTextUppercase~/~\MakeTextLowercase~
  - Tufte-style font size definitions (10pt/14pt baseline)
  - Fixed ~\makeatletter~/~\makeatother~ wrapping for font redefinitions
  - Added ~\newthought~ command for Tufte-style paragraph openers
  - Commented out ~\textbf~ redefinition that was causing bold to render as italic

* 2025-12-06: Fix paths and display settings
- Updated ~goals-agenda.el~ to use CloudStorage paths (~~/Library/CloudStorage/Dropbox/~)
- Changed master task list path from ~data/~ to ~goals/~ subdirectory
- Added "DELL U3223QE" to ~jay/notch-display-identifiers~ for external monitor support

* 2025-12-06: Improve double-line-breaks-in-region function
- Rewrote ~double-line-breaks-in-region~ in ~shared-functions.org~ to fix several issues:
  - Now normalizes Windows (~\r\n~) and old Mac (~\r~) line endings first
  - Collapses 3+ consecutive newlines to exactly 2
  - Only doubles single newlines, leaving existing double breaks unchanged
  - Fixed boundary handling so newlines at region start/end are processed
- Added keybinding ~s-k d l~ in ~gnu-emacs-startup.org~

* 2025-12-01: Add invisible space handling to html2org-clipboard
- Added ~\u202F~ (narrow no-break space) replacement to ~chatgpt2org.el~
- Added ~\u200B~ (zero-width space) removal to ~chatgpt2org.el~
- These characters were causing issues when pasting from Instagram DMs
- Already handled in ~pasteboard-copy-and-paste-functions.el~ via ~smart-quotes-replacement-pairs~ and ~replace-weird-spaces~, but ~html2org-clipboard~ uses a separate code path via ~chatgpt2org~

* 2026-04-11: Fix tangled parser break in visit-most-recent-file
- Fixed a startup-breaking parse error in [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.org][shared-functions.org]] inside ~visit-most-recent-file~
- Corrected an invalid escaped format string:
  - from ~\"#%d — %s\"~
  - to ~"#%d — %s"~
- Rebalanced the ~let*~ form that builds ~buffer-file-name-list~ and the fallback recent-file chooser
- Deleted the stale tangled file [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.el][shared-functions.el]], retangled from source, and rechecked batch parsing/load behavior
- Result: removed the ~Invalid read syntax: "#"~ and ~End of file during parsing~ failures coming from the tangled [[file+emacs:/Users/jay/emacs/emacs-settings/shared-functions.el][shared-functions.el]]

* Archive
See [[file:work-log-2025.org][work-log-2025.org]] for earlier entries.
